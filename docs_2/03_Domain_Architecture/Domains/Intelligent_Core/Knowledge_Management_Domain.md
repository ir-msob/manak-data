<div dir="rtl">

# معماری دامنه مدیریت دانش (Knowledge Management Domain Architecture)

**نام پلتفرم:** Enterprise AI Context & Automation Platform

**نسخه:** 1.2

**وضعیت:** بازبینی‌شده

**سند مرجع نام‌گذاری:** `Naming_And_Terminology_Glossary`

---

## ۱. هدف سند

این سند با هدف تعریف معماری دامنه **مدیریت دانش** (Knowledge Management Domain) تدوین شده است. این دامنه مسئول مدیریت ساختار دانش سازمانی، گراف دانش (Knowledge Graph)، هستان‌شناسی (Ontology)، موجودیت‌ها و روابط بین آن‌ها است. هدف آن ایجاد یک لایه معنایی غنی و قابل توسعه برای سازماندهی، یکپارچه‌سازی و بازیابی دانش به‌گونه‌ای است که درک عمیق‌تری از مفاهیم، روابط و وابستگی‌های سازمانی فراهم شود و زمینه‌های دقیق‌تری برای مدل‌های هوش مصنوعی تولید گردد.

این سند به‌عنوان مرجع اصلی برای تیم‌های توسعه، معماری، داده و هوش مصنوعی در زمینه طراحی و پیاده‌سازی زیرساخت دانش سازمانی و گراف دانش پلتفرم عمل می‌کند.

---

## ۲. دامنه سند

این سند تمام جنبه‌های مرتبط با دامنه مدیریت دانش را پوشش می‌دهد و شامل موارد زیر است:

- **هستان‌شناسی (Ontology):** تعریف مفاهیم، کلاس‌ها، ویژگی‌ها و روابط پایه سازمان
- **گراف دانش (Knowledge Graph):** ذخیره‌سازی و مدیریت موجودیت‌ها، روابط و فراداده‌های مرتبط
- **استخراج موجودیت (Entity Extraction):** شناسایی خودکار موجودیت‌ها از متون و داده‌های غیرساخت‌یافته
- **استخراج روابط (Relation Extraction):** شناسایی خودکار روابط بین موجودیت‌ها از متون و داده‌ها
- **مدیریت هستان‌شناسی:** نسخه‌بندی، تکامل و نگهداری هستان‌شناسی در طول زمان
- **پرس‌وجوی گراف (Graph Query):** ارائه رابط‌های پرس‌وجو برای بازیابی دانش مبتنی بر رابطه
- **یکپارچگی با RAG:** استفاده از گراف دانش برای غنی‌سازی بازیابی و تولید پاسخ
- **مشاهده‌پذیری و تحلیل:** ابزارهای بصری‌سازی و تحلیل گراف دانش
- **مدیریت چرخه عمر دانش:** ایجاد، به‌روزرسانی، نسخه‌بندی و حذف موجودیت‌ها و روابط

جزئیات پیاده‌سازی فنی و کدنویسی در اسناد سطح پایین‌تر تدوین می‌شوند. این سند صرفاً **معماری سطح بالا و اصول طراحی** دامنه را تعریف می‌کند.

---

## ۳. اصول طراحی دامنه

| اصل | توضیح |
| :--- | :--- |
| **مرجع واحد حقیقت (Single Source of Truth)** | هر مفهوم، موجودیت و رابطه در گراف دانش تنها یک بار تعریف و نگهداری می‌شود تا از تناقض و ابهام جلوگیری شود. |
| **تکامل‌پذیری هستان‌شناسی (Ontology Evolution)** | هستان‌شناسی باید قابلیت تغییر و تکامل تدریجی را داشته باشد بدون اینکه مصرف‌کنندگان موجود را دچار اختلال کند. |
| **هم‌راستایی با زبان مشترک (Ubiquitous Language)** | مفاهیم و روابط تعریف‌شده در هستان‌شناسی باید با زبان مشترک کسب‌وکار و دامنه‌های مختلف هم‌راستا باشند. |
| **غنی‌سازی خودکار (Automated Enrichment)** | تا حد امکان، استخراج موجودیت‌ها و روابط از متون و داده‌ها باید به‌صورت خودکار انجام شود تا هزینه نگهداری کاهش یابد. |
| **قابلیت رهگیری (Traceability)** | هر موجودیت و رابطه باید قابل ردیابی به منبع اصلی (سند، پایگاه داده، API) باشد تا شفافیت و اعتمادپذیری تضمین شود. |
| **مقیاس‌پذیری (Scalability)** | گراف دانش باید توانایی مدیریت میلیون‌ها موجودیت و رابطه را با تأخیر کم در پرس‌وجو داشته باشد. |
| **جداسازی سرویس‌ها (Service Decoupling)** | سرویس‌های مدیریت دانش باید مستقل از سایر دامنه‌ها طراحی شوند تا تغییرات در یک بخش تأثیری بر بخش دیگر نداشته باشد. |
| **حفاظت از داده (Data Protection)** | داده‌های حساس در گراف دانش باید با رمزنگاری، کنترل دسترسی و Masking محافظت شوند. |
| **Semantic Consistency (سازگاری معنایی)** | تمام موجودیت‌ها، روابط و ویژگی‌ها باید با هستان‌شناسی تعریف‌شده سازگار باشند و از ایجاد مفاهیم هم‌پوشان یا متناقض جلوگیری شود. |
| **Knowledge Provenance (منشأ دانش)** | علاوه بر منبع اولیه، مسیر تولید، پردازش و تغییرات هر موجودیت و رابطه باید ثبت شود تا منشأ کامل دانش قابل رهگیری باشد. |
| **Reasoning Ready (آمادگی برای استنتاج)** | ساختار گراف دانش باید به‌گونه‌ای طراحی شود که قابلیت انجام استنتاج، کشف روابط جدید و استدلال ماشینی را فراهم کند. |
| **Confidence Driven Knowledge (دانش مبتنی بر اطمینان)** | هر موجودیت و رابطه باید دارای امتیاز اطمینان باشد تا در تحلیل‌ها و بازیابی دانش مورد استفاده قرار گیرد. |
| **Incremental Evolution (تکامل تدریجی)** | تغییرات گراف دانش باید به‌صورت تدریجی و بدون نیاز به بازسازی کامل گراف اعمال شوند. |
| **مرزبندی صریح مالکیت کاربر بر موجودیت (Explicit User Ownership Boundary)** | مرز بین «موجودیتی که کاربر مالک/تولیدکننده آن است» (رابطه `owns` یا `generated_by`) و «موجودیتی که صرفاً به کاربر ارجاع می‌دهد» (رابطه `references` یا `related_to`) باید صریح و قابل پرس‌وجو باشد، زیرا این مرز، دامنه اجرای عملیات حذف داده کاربر (Right to be Forgotten) در گراف دانش را تعیین می‌کند (بخش ۴.۱.۳). |
| **Shared Kernel با دامنه دانش و زمینه** | مدل هستان‌شناسی (انواع Node، انواع Relation) به‌عنوان Shared Kernel رسمی بین این دامنه و دامنه دانش و زمینه تعریف می‌شود. طرحواره مرجع در `Data_Model_And_Knowledge_Schema_Overview` (بخش ۶) ثبت شده است. این دامنه مالک انحصاری پیاده‌سازی موتور گراف است و دامنه دانش و زمینه صرفاً مصرف‌کننده از طریق Graph Query Service است. |

---

## ۴. مؤلفه‌های اصلی دامنه

### ۴.۱. Ontology Manager (مدیر هستان‌شناسی)

**مسئولیت:** تعریف، نگهداری و تکامل هستان‌شناسی سازمان شامل کلاس‌ها، ویژگی‌ها و روابط پایه.

**وظایف کلیدی:**

- **تعریف کلاس‌ها:** تعریف کلاس‌های پایه (Person، Organization، Document، Service، Process، Rule، Role، Permission)
- **تعریف ویژگی‌ها:** تعریف ویژگی‌های هر کلاس (نام، توضیحات، مالک، تاریخ ایجاد)
- **تعریف روابط:** تعریف روابط بین کلاس‌ها (references، depends_on، owns، generated_by، related_to، has_role، has_permission، applies_to)
- **نسخه‌بندی هستان‌شناسی:** نگهداری نسخه‌های مختلف هستان‌شناسی با قابلیت بازگشت (طبق `Versioning_And_Compatibility_Strategy`)
- **اعتبارسنجی هستان‌شناسی:** بررسی سازگاری، عدم تناقض و کامل بودن هستان‌شناسی
- **انتشار هستان‌شناسی:** انتشار هستان‌شناسی به‌عنوان یک منبع قابل استفاده برای سایر دامنه‌ها از طریق رویداد `OntologyVersionPublished`

**فیلدهای کلیدی هستان‌شناسی:**

| فیلد | توضیح |
| :--- | :--- |
| `ontology_id` | شناسه یکتای هستان‌شناسی |
| `version` | نسخه هستان‌شناسی (Semantic Versioning) |
| `classes` | لیست کلاس‌ها با ویژگی‌ها و روابط |
| `relations` | لیست روابط با دامنه و برد |
| `status` | وضعیت (Draft, Active, Deprecated, Sunset, Retired) |
| `created_at` | زمان ایجاد |
| `updated_at` | زمان آخرین به‌روزرسانی |
| `owner` | مالک هستان‌شناسی |

---

#### ۴.۱.۱ مدیریت چرخه عمر هستان‌شناسی (Ontology Lifecycle Management)

هر نسخه هستان‌شناسی دارای چرخه عمر مشخص است (طبق `Versioning_And_Compatibility_Strategy` بخش ۴ و ۷):

| وضعیت | توضیح | مدت زمان |
| :--- | :--- | :--- |
| **Draft** | در حال طراحی، فقط در محیط Development قابل استفاده است. | تا زمان انتشار |
| **Published** | نسخه منتشرشده با برچسب `-beta` یا `-rc` برای آزمون در Staging. | حداکثر ۱ ماه |
| **Active** | نسخه پایدار و قابل استفاده در Production. | حداقل ۶ ماه |
| **Deprecated** | نسخه هنوز پشتیبانی می‌شود اما مصرف‌کنندگان تشویق به مهاجرت می‌شوند. | حداقل ۶ ماه |
| **Sunset** | نسخه فقط برای Queryهای خواندنی پشتیبانی می‌شود، نوشتن ممنوع است. | حداقل ۳ ماه |
| **Retired** | نسخه کاملاً حذف شده است. | — |

تمام تغییرات باید همراه با نسخه، دلیل تغییر و تأثیر بر دامنه‌های وابسته ثبت شوند.

---

#### ۴.۱.۲. انتشار و اطلاع‌رسانی نسخه‌های جدید هستان‌شناسی

هنگام انتشار یک نسخه جدید از هستان‌شناسی (به‌ویژه تغییرات Major)، Ontology Manager موظف است:

- نسخه جدید را در **Schema Registry** (سرویس مدیریت فراداده و دودمان) ثبت کند.
- یک رویداد **OntologyVersionPublished** با محموله شامل `ontology_id`, `new_version`, `deprecated_version`, `release_notes`, `deprecation_date` منتشر کند.
- حداقل ۶ ماه قبل از حذف یک نسخه، آن را به‌عنوان `Deprecated` اعلام کند.

مصرف‌کنندگان (به‌ویژه `Knowledge & Context Engine`) با اشتراک در این رویداد، از تغییرات مطلع شده و می‌توانند برنامه مهاجرت خود را تنظیم کنند.

---

#### ۴.۱.۳. مرز مالکیت کاربر بر موجودیت‌های گراف (User Ownership Boundary for Deletion)

برای پشتیبانی از الزام قانونی حق فراموشی (طبق `Data_Governance_and_Compliance` بخش ۱۲)، هستان‌شناسی باید به‌صورت صریح دو دسته رابطه را از یکدیگر تفکیک کند (طبق `Data_Model_And_Knowledge_Schema_Overview` بخش ۶.۲):

| دسته رابطه | نمونه | تأثیر بر حذف داده کاربر |
| :--- | :--- | :--- |
| **رابطه مالکیتی (Ownership Relations)** | `owns`، `generated_by` که مبدأ آن گره `Person` معادل کاربر درخواست‌دهنده حذف است | موجودیت مقصد این رابطه **در دامنه اجرای حذف قرار می‌گیرد** و باید توسط عملیات `knowledge_graph.delete_user_entities` (بخش ۴.۵.۱) حذف یا ناشناس شود. |
| **رابطه ارجاعی (Reference Relations)** | `references`، `related_to`، `depends_on` که کاربر صرفاً در آن‌ها ذکر شده است | موجودیت مقصد این رابطه **در دامنه اجرای حذف قرار نمی‌گیرد**؛ صرفاً ارجاع به گره `Person` مربوط به کاربر باید ناشناس یا حذف شود، نه خود موجودیت مرتبط. |

**الزام پیاده‌سازی:** عملیات `knowledge_graph.delete_user_entities` باید صرفاً موجودیت‌هایی را هدف قرار دهد که از طریق رابطه‌های مالکیتی (نه ارجاعی) به گره `Person` کاربر متصل‌اند. این مرز باید در Query استاندارد Endpoint حذف (بخش ۴.۵.۱) به‌صورت صریح پیاده‌سازی شود تا از حذف بیش‌ازحد (Over-deletion) داده‌های سایر کاربران که صرفاً به سند کاربر ارجاع داده‌اند، جلوگیری شود.

---

### ۴.۲. Knowledge Graph Engine (موتور گراف دانش)

> **یادداشت هم‌راستاسازی معماری:** این مؤلفه، تنها مالک و پیاده‌ساز گراف دانش در کل پلتفرم است. دامنه دانش و زمینه (`Knowledge_and_Context_Domain.md`) هیچ گراف، Entity Type یا Relation Type مستقلی ندارد و صرفاً از طریق `Graph Query Service` (بخش ۴.۵ همین سند) به این مؤلفه دسترسی دارد. مدل Ontology این بخش، Shared Kernel رسمی است و طرحواره مرجع آن در `Data_Model_And_Knowledge_Schema_Overview` بخش ۶ ثبت شده است.

**مسئولیت:** ذخیره‌سازی، مدیریت و پرس‌وجوی موجودیت‌ها، روابط و فراداده‌های گراف دانش.

**وظایف کلیدی:**

- **ذخیره‌سازی موجودیت‌ها:** ذخیره موجودیت‌ها با فراداده کامل (نوع، ویژگی‌ها، منبع)
- **ذخیره‌سازی روابط:** ذخیره روابط بین موجودیت‌ها با نوع، جهت و ویژگی‌های خاص
- **مدیریت ایندکس:** ایجاد ایندکس‌های مناسب برای جستجوی سریع موجودیت‌ها و روابط
- **پرس‌وجوی گراف:** پشتیبانی از پرس‌وجوهای گرافی (Cypher، SPARQL، Gremlin)
- **مدیریت نسخه:** نگهداری نسخه‌های مختلف موجودیت‌ها و روابط
- **مدیریت چرخه عمر:** بایگانی و حذف امن موجودیت‌ها و روابط منسوخ

**قابلیت‌های تکمیلی موتور گراف**

- Merge خودکار موجودیت‌های تکراری
- تشخیص تعارض بین موجودیت‌ها
- تشخیص چرخه‌های غیرمجاز
- مدیریت Super Nodeها
- مدیریت نسخه موجودیت‌ها
- نگهداری امتیاز اعتماد روابط
- نگهداری تاریخ اعتبار روابط

**پایگاه‌های داده گرافی پیشنهادی:**

| پایگاه داده | کاربرد | ویژگی‌ها |
| :--- | :--- | :--- |
| **Neo4j** | گراف دانش سازمانی | زبان Cypher، ACID، Community/Enterprise، پشتیبانی از Causal Cluster |
| **Amazon Neptune** | گراف دانش در ابر | پشتیبانی از Gremlin و SPARQL، مدیریت‌شده |
| **ArangoDB** | گراف دانش چندمدلی | پشتیبانی از JSON، گراف و جستجوی متن |
| **Memgraph** | گراف دانش با عملکرد بالا | سازگار با Cypher، In‑Memory با Persistence |

**نمونه مدل گراف (شکل‌واره):**

```text
(Person) -[works_in]-> (Organization)
(Person) -[owns]-> (Document)
(Person) -[has_role]-> (Role)
(Role) -[has_permission]-> (Permission)
(Document) -[references]-> (Document)
(Document) -[describes]-> (Service)
(Service) -[depends_on]-> (Service)
(Process) -[uses]-> (Tool)
(Rule) -[applies_to]-> (Service)
```

---

### ۴.۳. Entity Extractor (استخراج‌کننده موجودیت)

**مسئولیت:** شناسایی و استخراج خودکار موجودیت‌ها از متون، اسناد و داده‌های غیرساخت‌یافته.

**وظایف کلیدی:**

- **تشخیص موجودیت‌های نام‌دار (NER):** شناسایی افراد، سازمان‌ها، مکان‌ها، تاریخ‌ها و مفاهیم خاص
- **تشخیص موجودیت‌های دامنه‌ای:** شناسایی موجودیت‌های خاص هر دامنه (مثلاً سرویس‌ها، فرآیندها، ابزارها)
- **استخراج ویژگی‌ها:** استخراج ویژگی‌های مرتبط با هر موجودیت (نام، توضیحات، Metadata)
- **اعتبارسنجی موجودیت:** بررسی صحت و کامل بودن موجودیت‌های استخراج‌شده
- **یکپارچگی با گراف:** ثبت موجودیت‌های جدید در گراف دانش با ارجاع به منبع

**روش‌های استخراج:**

| روش | توضیح | کاربرد |
| :--- | :--- | :--- |
| **Rule‑Based** | استفاده از قواعد و الگوهای دست‌نویس | موجودیت‌های با ساختار مشخص |
| **ML‑Based (NER)** | استفاده از مدل‌های یادگیری ماشین | موجودیت‌های عمومی و دامنه‌ای |
| **LLM‑Based** | استفاده از مدل‌های زبانی بزرگ (Few‑Shot) | موجودیت‌های پیچیده و زمینه‌دار |
| **Hybrid** | ترکیب روش‌های مختلف | بهترین دقت و پوشش |

**مرحله نرمال‌سازی موجودیت‌ها**

پس از استخراج، موجودیت‌ها باید:

- استانداردسازی نام
- حذف تکرار
- تطبیق با موجودیت‌های موجود
- تخصیص شناسه یکتا
- تعیین سطح اطمینان
- اتصال به هستان‌شناسی

**ابزارهای پیشنهادی:** SpaCy، Stanza، Hugging Face Transformers، Flair

---

### ۴.۴. Relation Extractor (استخراج‌کننده روابط)

**مسئولیت:** شناسایی و استخراج خودکار روابط بین موجودیت‌ها از متون و داده‌ها.

**وظایف کلیدی:**

- **تشخیص روابط:** شناسایی روابط معنایی بین موجودیت‌ها (references، depends_on، owns)
- **طبقه‌بندی روابط:** تعیین نوع دقیق رابطه بر اساس هستان‌شناسی، به‌ویژه تفکیک صریح بین روابط مالکیتی و ارجاعی (طبق بخش ۴.۱.۳)
- **استخراج ویژگی‌های رابطه:** استخراج ویژگی‌های مرتبط با هر رابطه (نوع، جهت، قدرت)
- **اعتبارسنجی رابطه:** بررسی صحت و منطقی بودن رابطه
- **یکپارچگی با گراف:** ثبت روابط جدید در گراف دانش با ارجاع به منبع

**روش‌های استخراج روابط:**

| روش | توضیح | کاربرد |
| :--- | :--- | :--- |
| **Pattern‑Based** | استفاده از الگوهای زبانی | روابط با ساختار مشخص |
| **Supervised ML** | آموزش مدل بر روی داده‌های برچسب‌دار | روابط رایج و تکراری |
| **LLM‑Based** | استفاده از LLM برای استنتاج روابط | روابط پیچیده و ضمنی |
| **Graph‑Based** | استنتاج روابط از گراف موجود | تکمیل خودکار گراف |

**ویژگی‌های تکمیلی روابط**

برای هر رابطه اطلاعات زیر نگهداری می‌شود:

- Confidence Score
- جهت رابطه
- وزن رابطه
- زمان اعتبار
- منبع ایجاد
- روش استخراج
- نسخه رابطه

---

#### ۴.۴.۱ استنتاج دانش (Knowledge Reasoning)

سامانه باید امکان تولید دانش جدید بر اساس روابط موجود را فراهم کند.

قابلیت‌ها:

- استنتاج قوانین
- استنتاج روابط جدید
- کشف روابط غیرمستقیم
- استنتاج سلسله‌مراتب
- اعتبارسنجی نتایج استنتاج
- ثبت منشأ دانش استنتاج‌شده

---

### ۴.۵. Graph Query Service (سرویس پرس‌وجوی گراف)

**مسئولیت:** ارائه رابط‌های استاندارد و کارآمد برای پرس‌وجوی گراف دانش.

**وظایف کلیدی:**

- **پشتیبانی از زبان‌های پرس‌وجو:** پشتیبانی از Cypher، SPARQL، Gremlin و Query DSL اختصاصی
- **بهینه‌سازی پرس‌وجو:** بهینه‌سازی خودکار پرس‌وجوها برای عملکرد بالا
- **فیلتر کردن و صفحه‌بندی:** پشتیبانی از فیلتر بر اساس Metadata، زمان، نوع و سایر معیارها
- **Aggregation:** محاسبه آمار و جمع‌بندی روی گراف
- **یکپارچگی با Retrieval:** ارائه نتایج به Knowledge & Context Engine برای RAG
- **مدیریت نسخه:** پشتیبانی از نسخه‌های مختلف گراف دانش و مدیریت خطای `VERSION_MISMATCH`

**مثال پرس‌وجو (Cypher):**

```cypher
MATCH (p:Person)-[r:works_in]->(o:Organization)
WHERE o.name = 'تیم هوش مصنوعی'
RETURN p.name, r.role, o.name
```

**APIهای پیشنهادی:**

| API | توضیح |
| :--- | :--- |
| **REST API** | پرس‌وجوهای ساده و از پیش تعریف‌شده. **الزام:** هر درخواست باید شامل هدر یا پارامتر `graph-version` باشد تا موتور گراف دانش نسخه موردنظر را شناسایی کند. |
| **GraphQL** | پرس‌وجوهای انعطاف‌پذیر با انتخاب فیلدها. **الزام:** نسخه گراف دانش باید در Context درخواست ارسال شود. |
| **gRPC** | پرس‌وجوهای با کارایی بالا برای سرویس‌های داخلی. **الزام:** نسخه گراف دانش در Metadata درخواست ارسال می‌شود. |
| **JDBC/ODBC** | دسترسی از ابزارهای تحلیل و BI. **الزام:** نسخه گراف دانش به‌عنوان پارامتر اتصال (Connection Parameter) تنظیم می‌شود. |

**مدیریت نسخه در زمان اجرا:**

- اگر درخواست با نسخه‌ای ارسال شود که پشتیبانی نمی‌شود، Graph Query Service باید خطای `VERSION_MISMATCH` با کد وضعیت `409 Conflict` و لیست نسخه‌های پشتیبانی‌شده بازگرداند.
- مصرف‌کننده (مانند Knowledge & Context Engine) باید در صورت دریافت این خطا، درخواست را با نسخه پایدار قبلی (Fallback) تکرار کند یا درخواست را لغو کند (طبق `Versioning_And_Compatibility_Strategy` بخش ۷.۲).

**بهینه‌سازی پرس‌وجو**

- Query Cache
- Query Plan Optimization
- مسیر‌یابی بهینه
- محدودسازی عمق پیمایش
- زمان‌بندی اجرای Query
- ثبت آمار اجرای Query

---

#### ۴.۵.۱. Endpoint حذف موجودیت‌های کاربر (`knowledge_graph.delete_user_entities`)

**مسئولیت:** پیاده‌سازی Endpoint داخلی استاندارد Workflow حذف سرتاسری داده (طبق `Data_Governance_and_Compliance` بخش ۱۲.۶) به‌گونه‌ای که صرفاً موجودیت‌های در مرز مالکیتی کاربر (بخش ۴.۱.۳) حذف یا ناشناس شوند.

**منطق اجرا:**

```text
دریافت user_id از Data Subject Request Service
        │
        ▼
یافتن گره Person متناظر با user_id
        │
        ▼
پیمایش صرفاً رابطه‌های مالکیتی (owns, generated_by) از این گره
        │
        ▼
برای هر موجودیت مقصد:
        │
        ├── اگر موجودیت فقط از طریق رابطه مالکیتی این کاربر قابل دسترسی است → حذف کامل
        │
        └── اگر موجودیت از طریق رابطه‌های ارجاعی سایر کاربران نیز به گراف متصل است → عدم حذف؛ صرفاً قطع رابطه مالکیتی این کاربر
        │
        ▼
ناشناس‌سازی یا حذف گره Person
        │
        ▼
گزارش تعداد موجودیت/رابطه حذف یا ناشناس‌شده به Data Subject Request Service
```

**ورودی/خروجی:** مطابق قرارداد استاندارد Endpoint حذف تعریف‌شده در `Data_Governance_and_Compliance` (بخش ۱۲.۶).

این Endpoint باید Idempotent باشد (طبق الزام عمومی بخش ۱۲.۶ سند `Data_Governance_and_Compliance`).

---

### ۴.۶. Knowledge Versioning (نسخه‌بندی دانش)

**مسئولیت:** مدیریت نسخه‌های مختلف موجودیت‌ها، روابط و هستان‌شناسی برای بازتولید و رهگیری تغییرات.

**وظایف کلیدی:**

- **نسخه‌بندی موجودیت‌ها:** نگهداری تاریخچه تغییرات هر موجودیت
- **نسخه‌بندی روابط:** نگهداری تاریخچه تغییرات هر رابطه
- **نسخه‌بندی هستان‌شناسی:** نگهداری نسخه‌های مختلف هستان‌شناسی (طبق `Versioning_And_Compatibility_Strategy`)
- **بازتولید (Reproducibility):** امکان بازگردانی وضعیت گراف به یک نسخه خاص
- **مقایسه نسخه‌ها:** نمایش تفاوت‌ها بین نسخه‌های مختلف

---

#### ۴.۶.۱ مدیریت کیفیت دانش (Knowledge Quality Management)

برای هر موجودیت و رابطه شاخص‌های کیفیت نگهداری می‌شود.

شاخص‌ها:

- Completeness
- Consistency
- Confidence
- Freshness
- Provenance
- Usage Frequency
- آخرین اعتبارسنجی

---

### ۴.۷. Graph Visualization & Exploration (بصری‌سازی و کاوش گراف)

**مسئولیت:** ارائه ابزارهای بصری برای مشاهده، کاوش و تحلیل گراف دانش.

**وظایف کلیدی:**

- **بصری‌سازی گراف:** نمایش موجودیت‌ها و روابط به‌صورت گراف تعاملی
- **فیلتر و جستجو:** امکان جستجو و فیلتر موجودیت‌ها و روابط
- **تحلیل مسیر:** نمایش مسیرهای بین دو موجودیت
- **تحلیل خوشه‌ها:** شناسایی خوشه‌ها و الگوهای ساختاری
- **گزارش‌گیری:** تولید گزارش از ساختار و محتوای گراف

**ابزارهای پیشنهادی:** Neo4j Bloom، Gephi، GraphXR، KeyLines

---

## ۵. جریان داده و فرآیند (Data & Process Flow)

جریان کامل از استخراج تا استفاده از گراف دانش:

```text
Source Data (Documents, Databases, APIs)
        │
        ▼
Entity Extraction (NER, LLM, Rules)
        │
        ├── Validate Entities
        │
        ├── Add Metadata (Source, Confidence, Timestamp)
        │
        ▼
Relation Extraction (Patterns, ML, LLM)
        │
        ├── Validate Relations
        │
        ├── Classify Ownership vs Reference (طبق بخش ۴.۱.۳)
        │
        ├── Add Metadata (Source, Confidence, Timestamp)
        │
        ▼
Knowledge Graph Store
        │
        ├── Merge with Existing (deduplication, conflict resolution)
        │
        ├── Update Versioning (new version)
        │
        ▼
Graph Indexing & Optimization
        │
        │
        ▼
Consumers
        │
        ├── Knowledge & Context Engine (for RAG, semantic search)
        │
        ├── Memory & Experience (for pattern storage)
        │
        ├── Agents (for decision making)
        │
        └── Analytics & Reporting (for insights)
```

---

### ۵.۱ چرخه مدیریت دانش

هر موجودیت و رابطه مراحل زیر را طی می‌کند.

1. استخراج
2. اعتبارسنجی
3. نرمال‌سازی
4. اتصال به هستان‌شناسی
5. ثبت در گراف
6. نسخه‌بندی
7. انتشار
8. بایگانی یا حذف

---

## ۶. قوانین کسب‌وکار مرتبط

| شناسه | عنوان | شرح | اولویت |
| :--- | :--- | :--- | :--- |
| BR‑085 | یکتایی موجودیت | هر موجودیت در گراف دانش باید یکتا باشد و از تکرار جلوگیری شود. | بالا |
| BR‑086 | اعتبارسنجی روابط | روابط بین موجودیت‌ها باید با هستان‌شناسی سازگار باشند. | بالا |
| BR‑087 | منبع‌یابی موجودیت‌ها | هر موجودیت و رابطه باید قابل ردیابی به منبع اصلی باشد. | بالا |
| BR‑088 | نسخه‌بندی هستان‌شناسی | هر تغییر در هستان‌شناسی باید با نسخه‌بندی ثبت شود (طبق `Versioning_And_Compatibility_Strategy`). | بالا |
| BR‑089 | حداقل کیفیت برای استخراج | موجودیت‌ها و روابط استخراج‌شده باید دارای امتیاز اطمینان (Confidence) بالای آستانه باشند. | بالا |
| BR‑090 | حذف امن موجودیت‌ها | موجودیت‌های حذف‌شده باید با روش‌های امن حذف شوند. | بالا |
| BR‑091 | دسترسی به گراف بر اساس نقش | دسترسی به موجودیت‌ها و روابط گراف باید بر اساس نقش کاربر کنترل شود. | بحرانی |
| BR‑092 | تکامل هستان‌شناسی | هستان‌شناسی باید قابلیت تکامل تدریجی را بدون شکستن مصرف‌کنندگان داشته باشد. | بالا |
| BR‑093 | مرزبندی صریح حذف داده کاربر در گراف | عملیات `knowledge_graph.delete_user_entities` مجاز به حذف موجودیت‌هایی نیست که صرفاً از طریق رابطه ارجاعی به کاربر متصل‌اند؛ فقط موجودیت‌های متصل از طریق رابطه مالکیتی حذف می‌شوند. | بحرانی |
| BR‑086 | دوره انسوخ‌سازی هستان‌شناسی | هر نسخه Major از هستان‌شناسی که منسوخ می‌شود، باید حداقل ۶ ماه قبل از حذف، به‌عنوان Deprecated اعلام شود. | بالا |
| BR‑087 | پشتیبانی از نسخه‌های گراف دانش | موتور دانش و زمینه باید حداقل از ۲ نسخه Major اخیر گراف دانش پشتیبانی کند. | بالا |
| BR‑088 | Fallback در صورت عدم تطابق نسخه گراف دانش | در صورت عدم تطابق نسخه گراف دانش، موتور دانش و زمینه باید به‌طور خودکار به نسخه پایدار قبلی Fallback کند. | بالا |
| BR‑089 | دسترسی انحصاری به گراف دانش از طریق موتور دانش | تمام درخواست‌های مربوط به داده‌های گراف دانش (از جمله از سوی Agentها) باید منحصراً از طریق موتور دانش و زمینه انجام شود. دسترسی مستقیم Agentها یا هر دامنه‌ی دیگری به Graph Query Service ممنوع است. | بحرانی |

> **ارجاع:** قوانین کامل در سند `Business_Rule_Catalog` ثبت شده‌اند.

---

### ۶.۱ قوانین کیفیت گراف دانش

| قانون | شرح |
| :--- | :--- |
| Entity Uniqueness | هر موجودیت باید یکتا باشد. |
| Relation Consistency | روابط باید با هستان‌شناسی سازگار باشند. |
| Mandatory Provenance | تمام موجودیت‌ها باید منشأ مشخص داشته باشند. |
| Confidence Threshold | موجودیت‌های با امتیاز پایین نباید منتشر شوند. |
| Ontology Compatibility | تمام موجودیت‌ها باید با نسخه فعال هستان‌شناسی سازگار باشند. |
| Ownership/Reference Distinction | هر رابطه باید به‌صراحت به‌عنوان مالکیتی یا ارجاعی طبقه‌بندی شود (بخش ۴.۱.۳). |

---

## ۷. رویدادهای دامنه (Domain Events)

| شناسه | نام رویداد | نسخه | دامنه تولیدکننده | دامنه‌های مصرف‌کننده | محرک | معنای کسب‌وکار | محموله کلیدی | الگوی ارتباطی |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| EVT-086 | **EntityExtracted** | v1.0 | مدیریت دانش | Knowledge Graph Engine | شناسایی یک موجودیت جدید از منبع | یک موجودیت جدید از منبع استخراج شد. | `entity_id`, `entity_type`, `source_document_id`, `confidence_score`, `metadata` | غیرهمزمان |
| EVT-087 | **RelationExtracted** | v1.0 | مدیریت دانش | Knowledge Graph Engine | شناسایی یک رابطه جدید بین موجودیت‌ها | یک رابطه جدید بین موجودیت‌ها استخراج شد. | `relation_id`, `source_entity_id`, `target_entity_id`, `relation_type`, `confidence_score` | غیرهمزمان |
| EVT-088 | **GraphUpdated** | v1.0 | مدیریت دانش | دانش و زمینه، حافظه و تجربه | به‌روزرسانی گراف دانش (موجودیت/رابطه جدید یا تغییر) | گراف دانش به‌روزرسانی شد (موجودیت/رابطه جدید یا تغییر). | `graph_version`, `updated_entities`, `updated_relations`, `change_type` | غیرهمزمان |
| EVT-089 | **OntologyChanged** | v1.0 | مدیریت دانش | همه دامنه‌ها | انتشار یک نسخه جدید از هستان‌شناسی | هستان‌شناسی تغییر کرد (نسخه جدید منتشر شد). | `ontology_id`, `new_version`, `deprecated_version`, `release_notes`, `deprecation_date` | غیرهمزمان |
| EVT-090 | **GraphQueryExecuted** | v1.0 | مدیریت دانش | مشاهده‌پذیری | اجرای یک پرس‌وجوی گراف | یک پرس‌وجوی گراف اجرا شد. | `query_id`, `graph_version`, `query_type`, `duration_ms`, `result_count` | غیرهمزمان |
| EVT-091 | **UserEntitiesDeleted** | v1.0 | مدیریت دانش | حاکمیت و انطباق، مشاهده‌پذیری | حذف موجودیت‌های در مرز مالکیتی یک کاربر مشخص | موجودیت‌های در مرز مالکیتی یک کاربر مشخص از گراف دانش حذف یا ناشناس شدند. | `user_id`, `deleted_entities_count`, `anonymized_relations_count`, `correlation_id` | غیرهمزمان |
| EVT-007 | **OntologyVersionPublished** | v1.0 | مدیریت دانش | دانش و زمینه، حاکمیت و انطباق | انتشار یک نسخه جدید از هستان‌شناسی | نسخه جدید هستان‌شناسی با تغییرات Major/Minor منتشر شد و مصرف‌کنندگان باید از آن مطلع شوند. | `ontology_id`, `new_version`, `deprecated_version`, `release_notes`, `deprecation_date`, `compatibility_notes` | غیرهمزمان |

> **ارجاع:** رویدادهای کامل در سند `Domain_Events_Catalog` ثبت شده‌اند.

---

### ۷.۱ رویدادهای تکمیلی

| رویداد | شرح |
| :--- | :--- |
| OntologyPublished | نسخه جدید هستان‌شناسی منتشر شد. |
| EntityMerged | دو موجودیت ادغام شدند. |
| EntityNormalized | نرمال‌سازی موجودیت انجام شد. |
| RelationValidated | رابطه اعتبارسنجی شد. |
| KnowledgeReasoned | دانش جدید از طریق استنتاج ایجاد شد. |
| GraphOptimized | بهینه‌سازی گراف انجام شد. |
| GraphIndexed | ایندکس گراف به‌روزرسانی شد. |
| KnowledgeArchived | دانش بایگانی شد. |

---

## ۸. وابستگی‌های دامنه

| دامنه تأمین‌کننده | نوع وابستگی | شدت | شرح |
| :--- | :--- | :--- | :--- |
| **مهندسی داده** | داده‌ای — زمان اجرا | قوی | تأمین داده‌های خام و پردازش‌شده برای استخراج موجودیت و رابطه |
| **دانش و زمینه** | مصرف‌کننده — زمان اجرا | قوی | مصرف‌کننده Graph Query Service برای غنی‌سازی Retrieval و RAG (این دامنه ارائه‌دهنده است، نه وابسته؛ ردیف جهت اطلاع از الگوی مصرف نگهداری می‌شود) |
| **حافظه و تجربه** | سرویس — زمان اجرا | متوسط | ذخیره الگوها و حقایق استخراج‌شده از گراف |
| **هویت و دسترسی** | امنیتی — زمان اجرا | قوی | کنترل دسترسی به گراف دانش و مدیریت هستان‌شناسی |
| **امنیت و حریم خصوصی** | سرویس — زمان اجرا | قوی | حفاظت از داده‌های حساس در گراف |
| **مشاهده‌پذیری** | رویدادی — زمان اجرا | ضعیف | ارسال Metrics و لاگ‌های پرس‌وجو و به‌روزرسانی |
| **حاکمیت و انطباق** | سرویس — زمان اجرا | متوسط | اعمال Policyهای نگهداری، حریم خصوصی و انطباق؛ فراخوانی Endpoint حذف (بخش ۴.۵.۱) در چارچوب Workflow حذف سرتاسری داده |
| **مدیریت فراداده و دودمان** | سرویس — زمان اجرا | متوسط | ثبت نسخه‌های هستان‌شناسی در Schema Registry و دریافت Metadata برای گزارش‌دهی انطباق |

> **ارجاع:** وابستگی‌های کامل در سند `Domain_Dependency_Map` ثبت شده‌اند.

---

### ۸.۱ وابستگی‌های زیرساختی

دامنه مدیریت دانش برای عملکرد صحیح به زیرساخت‌های زیر وابسته است.

- Graph Database
- Vector Database
- Object Storage
- Metadata Catalog
- Search Engine
- Identity Provider
- Monitoring Platform
- Secret Management
- Message Broker

---

## ۹. ارتباط با سایر اسناد (References)

| نام سند | نوع ارتباط |
| :--- | :--- |
| `Naming_And_Terminology_Glossary` | مرجع واژه‌نامه و استاندارد نام‌گذاری |
| `Domain_Landscape` | مرجع دامنه‌های اصلی و جایگاه این دامنه |
| `Context_Map` | مرجع الگوهای تعامل بین دامنه‌ها (این دامنه با دانش و زمینه از طریق Shared Kernel تعامل دارد) |
| `Domain_Dependency_Map` | مرجع وابستگی‌های این دامنه به سایر دامنه‌ها |
| `Business_Rule_Catalog` | مرجع قوانین کسب‌وکار مرتبط |
| `Domain_Events_Catalog` | مرجع رویدادهای دامنه |
| `Architecture_Overview` | مرجع Componentها و لایه‌های معماری |
| `Data_Engineering_Domain_Architecture` | مرجع مهندسی داده که داده‌های ورودی را تأمین می‌کند |
| `Knowledge_and_Context_Domain.md` | مرجع دامنه دانش و زمینه؛ مصرف‌کننده Graph Query Service این دامنه از طریق Shared Kernel رسمی تعریف‌شده در `Data_Model_And_Knowledge_Schema_Overview` (بخش ۶) — این دامنه هرگز از دانش‌وزمینه داده گرافی دریافت نمی‌کند، فقط ارائه می‌دهد |
| `Memory_and_Experience_Domain.md` | مرجع دامنه حافظه و تجربه که الگوهای گراف را ذخیره می‌کند |
| `Data_Model_And_Knowledge_Schema_Overview` | مرجع مدل داده و Shared Kernel که گراف دانش بر اساس آن شکل گرفته است |
| `Security_and_Privacy_Architecture` | مرجع کنترل‌های امنیتی و دسترسی |
| `Monitoring_and_Observability_Overview` | مرجع پایش و مشاهده‌پذیری |
| `Data_Governance_and_Compliance` | مرجع کامل فرآیند حق فراموشی (بخش ۱۲) که مرز مالکیت کاربر (بخش ۴.۱.۳) و Endpoint حذف (بخش ۴.۵.۱) همین سند پیاده‌سازی آن در گراف دانش است |
| `Versioning_And_Compatibility_Strategy` | مرجع استراتژی مهاجرت که بخش ۴.۱.۱ و ۴.۱.۲ بر اساس آن طراحی شده است |
| `Metadata_and_Lineage_Domain.md` | مرجع Schema Registry که نسخه‌های هستان‌شناسی در آن ثبت می‌شوند |

---

### ۹.۱ شاخص‌های کلیدی عملکرد (KPI)

| شاخص | هدف |
| :--- | :--- |
| Graph Query Latency | کمتر از 300ms |
| Entity Extraction Precision | بیش از 90% |
| Relation Extraction Precision | بیش از 90% |
| Knowledge Coverage | روند افزایشی |
| Duplicate Entity Rate | کمتر از 1% |
| Ontology Consistency | 100% |
| Graph Availability | بیش از 99.9% |
| Reasoning Success Rate | بیش از 95% |
| Provenance Coverage | 100% |
| **Ownership/Reference Classification Coverage** | **100% روابط متصل به گره Person** |
| **Ontology Version Migration Success** | بیش از 99% |

---

## ۱۰. نتیجه‌گیری

دامنه **مدیریت دانش** به‌عنوان **لایه معنایی سازمان**، نقش حیاتی در سازماندهی، یکپارچه‌سازی و غنی‌سازی دانش سازمانی ایفا می‌کند. معماری تعریف‌شده در این سند، چارچوبی استاندارد، مقیاس‌پذیر، قابل تکامل و امن برای مدیریت هستان‌شناسی، گراف دانش، استخراج موجودیت‌ها و روابط، و ارائه پرس‌وجوی کارآمد فراهم می‌آورد. مرزبندی صریح روابط مالکیتی/ارجاعی (بخش ۴.۱.۳) تضمین می‌کند که این دامنه بتواند به‌طور دقیق و بدون ریسک حذف بیش‌ازحد، در فرآیند سرتاسری حق فراموشی پلتفرم مشارکت کند. همچنین، یکپارچگی با استراتژی نسخه‌گذاری و مهاجرت (بخش ۴.۱.۱ و ۴.۱.۲) تضمین می‌کند که تغییرات در هستان‌شناسی به‌صورت کنترل‌شده و بدون ایجاد اختلال در سرویس‌های مصرف‌کننده مدیریت شوند.

رعایت اصول طراحی (Single Source of Truth، Ontology Evolution، Automated Enrichment، Traceability، Explicit User Ownership Boundary) و پیاده‌سازی مؤلفه‌های کلیدی (Ontology Manager، Knowledge Graph Engine، Entity Extractor، Relation Extractor، Graph Query Service) تضمین می‌کند که پلتفرم بتواند دانش سازمانی را به‌صورت ساختاریافته، غنی و قابل استفاده برای مدل‌های هوش مصنوعی و سرویس‌های مختلف مدیریت کند و زمینه‌های دقیق‌تری برای تصمیم‌گیری و استدلال فراهم آورد.

**مسئولیت به‌روزرسانی:** تیم مدیریت دانش به همراه معماران ارشد، تیم داده و تیم هوش مصنوعی مسئول بازبینی دوره‌ای این سند، به‌روزرسانی آن بر اساس تغییرات هستان‌شناسی، نیازمندی‌های جدید و بازخورد تیم‌های مصرف‌کننده هستند.

**ثبات سند:** این سند باید با تغییرات در معماری کلان، نیازمندی‌های دانش و پیشرفت‌های فناوری‌های استخراج دانش هم‌گام باشد. هر تغییر در ساختار گراف دانش یا هستان‌شناسی باید در این سند منعکس شود تا سایر اسناد معماری و طراحی نیز به‌روزرسانی شوند.

</div>