<div dir="rtl">

# نقشه کانتکست دامنه‌ها (Context Map)

**نام پلتفرم:** Enterprise AI Context & Automation Platform

**نسخه:** 1.3

**وضعیت:** بازبینی‌شده

**سند مرجع نام‌گذاری:** `Naming_And_Terminology_Glossary`

---

## ۱. هدف سند

این سند با هدف ترسیم **نقشه کانتکست** (Context Map) دامنه‌های پلتفرم تدوین شده است. نقشه کانتکست، ارتباطات، مرزها و الگوهای تعامل بین دامنه‌های مختلف (Bounded Contextها) را مشخص می‌کند و نشان می‌دهد که هر دامنه با کدام دامنه‌های دیگر در تعامل است، چه نوع رابطه‌ای بین آن‌ها برقرار است و چه قراردادها و انتظاراتی بین آن‌ها وجود دارد.

این سند با بهره‌گیری از مفاهیم **طراحی مبتنی بر دامنه** (Domain-Driven Design — DDD)، به شفاف‌سازی مرزهای حوزه‌های مختلف، مدیریت وابستگی‌ها و جلوگیری از نشت مفاهیم بین دامنه‌ها کمک می‌کند. این سند به‌عنوان مرجع اصلی برای تیم‌های معماری، توسعه و محصول در زمینه درک تعاملات بین دامنه‌ها و طراحی رابط‌های بین آن‌ها عمل می‌کند.

---

## ۲. دامنه سند

این سند تمام دامنه‌های تعریف‌شده در سند `Domain Landscape` را پوشش می‌دهد و شامل موارد زیر است:

- **نقشه ارتباطات:** نمایش گرافیکی و توصیفی روابط بین دامنه‌ها
- **الگوهای یکپارچه‌سازی:** الگوهای تعامل (مشتری-تأمین‌کننده، مشارکت، هسته مشترک و غیره)
- **قراردادها و انتظارات:** خروجی‌ها، رویدادها و APIهای مورد انتظار بین دامنه‌ها
- **مرزهای معنایی:** مشخص کردن اینکه چه مفاهیمی در کدام دامنه تعریف و مدیریت می‌شوند

جزئیات پیاده‌سازی فنی و مشخصات دقیق APIها در اسناد تخصصی هر دامنه و سند `API_Contract_Overview` تدوین می‌شوند. این سند صرفاً **الگوهای تعامل سطح بالا** را تعریف می‌کند.

---

## ۳. مفاهیم کلیدی نقشه کانتکست

| مفهوم | توضیح |
| :--- | :--- |
| **Bounded Context (کانتکست محدود)** | مرز مشخصی که در آن یک مدل دامنه تعریف و معنا دارد. هر دامنه یک Bounded Context مستقل است. |
| **Upstream / Downstream** | جهت وابستگی: دامنه Upstream تأمین‌کننده خدمات یا داده است و دامنه Downstream مصرف‌کننده آن. |
| **Contract (قرارداد)** | مجموعه انتظارات، خروجی‌ها، رویدادها و APIهایی که یک دامنه در اختیار سایر دامنه‌ها قرار می‌دهد. |
| **Anticorruption Layer (لایه ضدفساد)** | لایه‌ای که از نشت مفاهیم و مدل‌های خارجی به دامنه داخلی جلوگیری می‌کند. |
| **Shared Kernel (هسته مشترک)** | مجموعه‌ای از مفاهیم و مدل‌ها که بین دو یا چند دامنه به‌اشتراک گذاشته می‌شوند. در این پلتفرم، مدل‌های پایه هویت (`User`, `Role`, `Permission`) و سیاست امنیتی (`Policy`, `Rule`) به‌عنوان Shared Kernel بین دامنه‌های «هویت و دسترسی» و «امنیت و حریم خصوصی» تعریف شده‌اند (طبق `Data_Model_And_Knowledge_Schema_Overview` بخش ۶). |
| **Conformist (پیرو)** | دامنه‌ای که مدل خود را با مدل دامنه Upstream هم‌راستا می‌کند (زمانی که تغییر در Upstream دشوار است). |
| **Open Host Service (سرویس میزبان باز)** | سرویسی که با ارائه یک رابط استاندارد و عمومی، امکان تعامل با دامنه‌های مختلف را فراهم می‌کند. |
| **Published Language (زبان منتشرشده)** | زبان مشترک و استانداردی که برای ارتباط بین دامنه‌ها استفاده می‌شود (معمولاً به‌صورت JSON Schema، ProtoBuf و غیره). |

---

## ۴. اصول طراحی نقشه کانتکست

| اصل | توضیح |
| :--- | :--- |
| **مرزهای واضح (Clear Boundaries)** | هر دامنه مرز مشخصی دارد و مفاهیم آن نباید به‌طور مستقیم به سایر دامنه‌ها نشت کنند. |
| **وابستگی حداقلی (Minimal Coupling)** | دامنه‌ها باید حداقل وابستگی مستقیم به یکدیگر داشته باشند و از طریق رابط‌های استاندارد تعامل کنند. |
| **استقلال تکاملی (Evolutionary Independence)** | هر دامنه باید قابلیت تکامل مستقل از سایر دامنه‌ها را داشته باشد. تغییرات در یک دامنه نباید مستقیماً سایر دامنه‌ها را تحت تأثیر قرار دهد. |
| **انتقال رویدادمحور (Event-Driven Communication)** | ارتباط بین دامنه‌ها تا حد امکان از طریق رویدادها انجام می‌شود تا وابستگی زمانی و مستقیم کاهش یابد. در مواردی که دو دامنه به یکدیگر نیاز متقابل دارند (مانند عامل‌ها و مدیریت جریان کار)، جهت ارتباط باید به‌گونه‌ای تفکیک شود که حداکثر یک مسیر همزمان و مستقیم باقی بماند و مسیر دیگر با مکانیزم تأیید دریافت (Acknowledgment) همراه باشد. |
| **قراردادهای صریح (Explicit Contracts)** | تمام تعاملات بین دامنه‌ها باید دارای قراردادهای مشخص، مستند و نسخه‌بندی‌شده باشند. |
| **حفظ یکپارچگی معنایی (Semantic Integrity)** | هر مفهوم باید در یک دامنه تعریف و مدیریت شود و سایر دامنه‌ها از طریق رابط‌های استاندارد به آن دسترسی داشته باشند. |

---

## ۵. نقشه ارتباطات دامنه‌ها (Domain Interaction Map)

### ۵.۱. نمودار کلی ارتباطات

```text
┌─────────────────────────────────────────────────────────────────────────────┐
│                         لایه دسترسی و تعامل                                │
│                    (Access & Interaction Layer)                            │
│                                                                             │
│  ┌──────────────────┐      ┌──────────────────┐      ┌──────────────────┐  │
│  │   دامنه          │      │   دامنه          │      │   دامنه          │  │
│  │ یکپارچه‌سازی و   │◄────►│  Webhook و       │◄────►│  اتصال‌دهنده‌ها  │  │
│  │  API             │      │  رویداد          │      │  (Connectors)    │  │
│  └────────┬─────────┘      └────────┬─────────┘      └────────┬─────────┘  │
│           │                         │                         │             │
│           └─────────────────────────┼─────────────────────────┘             │
│                                     │                                       │
└─────────────────────────────────────┼───────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                           لایه هسته هوشمند                                   │
│                     (Intelligent Core Layer)                                │
│                                                                             │
│  ┌──────────────────┐      ┌──────────────────┐      ┌──────────────────┐   │
│   │   دامنه          │      │   دامنه          │      │   دامنه          │  │
│   │  عامل‌ها و        │◄────►│  دانش و          │◄────►│  حافظه و         │  │
│   │  هماهنگی         │      │  زمینه           │      │  تجربه           │  │
│  └────────┬─────────┘      └────────┬─────────┘      └────────┬─────────┘   │
│           │                         │                         │             │
│           │                         │                         │             │
│  ┌────────┴─────────┐      ┌────────┴─────────┐      ┌────────┴─────────┐   │
│   │   دامنه          │      │   دامنه          │      │   دامنه          │  │
│   │  ابزارها و       │◄────►│  مدیریت          │◄────►│  مدیریت          │  │
│   │  اقدامات         │      │  مدل‌ها           │      │  جریان کار       │  │
│  └──────────────────┘      └──────────────────┘      └──────────────────┘   │
│                                                                             │
└─────────────────────────────────────┬───────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                           لایه داده و هوش مصنوعی                             │
│                     (Data & AI Layer)                                       │
│                                                                             │
│  ┌──────────────────┐      ┌──────────────────┐      ┌──────────────────┐   │
│   │   دامنه          │      │   دامنه          │      │   دامنه          │  │
│   │  مهندسی          │◄────►│  یادگیری         │◄────►│  مدیریت          │  │
│   │  داده            │      │  ماشین           │      │  دانش            │  │
│  └──────────────────┘      └──────────────────┘      └──────────────────┘   │
│                                                                             │
└─────────────────────────────────────┬───────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                           لایه زیرساخت و حاکمیت                              │
│                   (Infrastructure & Governance Layer)                       │
│                                                                             │
│  ┌──────────────────┐      ┌──────────────────┐      ┌──────────────────┐   │
│   │   دامنه          │      │   دامنه          │      │   دامنه          │  │
│   │  هویت و          │◄────►│  امنیت و         │◄────►│  حاکمیت و        │  │
│   │  دسترسی          │      │   حریم خصوصی     │      │   انطباق         │  │
│  └────────┬─────────┘      └────────┬─────────┘      └────────┬─────────┘   │
│           │                         │                         │             │
│  ┌────────┴─────────┐      ┌────────┴─────────┐      ┌────────┴─────────┐   │
│   │   دامنه          │      │   دامنه          │      │   دامنه          │  │
│   │  زیرساخت و       │◄────►│   مشاهده‌پذیری    │◄────►│  هوش مصنوعی      │  │
│   │  عملیات          │      │                  │      │   مسئولانه        │  │
│  └──────────────────┘      └──────────────────┘      └──────────────────┘   │
│                                                                             │
│  ┌──────────────────────────────────────────────────────────────────────┐    │
│   │                       دامنه درخواست‌های موضوع داده                   │    │
│   │                   (Data Subject Requests Domain)                    │    │
│   └──────────────────────────────────────────────────────────────────────┘    │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## ۶. الگوهای تعامل بین دامنه‌ها

### ۶.۱. لایه دسترسی و تعامل

| دامنه مبدأ | دامنه مقصد | الگوی تعامل | جهت وابستگی | قراردادهای کلیدی |
| :--- | :--- | :--- | :--- | :--- |
| **یکپارچه‌سازی و API** | **عامل‌ها و هماهنگی** | **Open Host Service** | Upstream → Downstream | درخواست‌های REST، WebSocket، گراف‌QL |
| **Webhook و رویداد** | **یکپارچه‌سازی و API** | **Conformist** | Upstream → Downstream | رویدادهای استاندارد، Webhook Callback |
| **اتصال‌دهنده‌ها** | **یکپارچه‌سازی و API** | **Anticorruption Layer** | Upstream → Downstream | داده‌های نرمال‌شده، Metadata |
| **یکپارچه‌سازی و API** | **هویت و دسترسی** | **Open Host Service** | Downstream → Upstream | توکن JWT، درخواست احراز هویت |
| **یکپارچه‌سازی و API** | **حاکمیت و انطباق** | **Conformist** | Downstream → Upstream | سیاست‌های دسترسی، Rate Limiting |

---

### ۶.۲. لایه هسته هوشمند

| دامنه مبدأ | دامنه مقصد | الگوی تعامل | جهت وابستگی | قراردادهای کلیدی |
| :--- | :--- | :--- | :--- | :--- |
| **عامل‌ها و هماهنگی** | **دانش و زمینه** | **Open Host Service** | Downstream → Upstream | جستجوی دانش، مونتاژ Context |
| **عامل‌ها و هماهنگی** | **مدیریت مدل‌ها** | **Open Host Service** | Downstream → Upstream | انتخاب مدل، فراخوانی مدل |
| **عامل‌ها و هماهنگی** | **ابزارها و اقدامات** | **Open Host Service** | Downstream → Upstream | اجرای Tool، وضعیت اجرا |
| **عامل‌ها و هماهنگی** | **حافظه و تجربه** | **Open Host Service** | Downstream → Upstream | ذخیره/بازیابی Memory، خواندن/نوشتن Shared Agent Context (میزبانی‌شده در `session-memory-service`) |
| **عامل‌ها و هماهنگی** | **مدیریت جریان کار** | **Open Host Service (همزمان، یک‌طرفه)** | Downstream → Upstream | آغاز اجرای Workflow و دریافت نتیجه/شناسه پیگیری از طریق REST؛ این تنها مسیر فراخوانی همزمان مستقیم بین این دو دامنه است. |
| **مدیریت جریان کار** | **عامل‌ها و هماهنگی** | **Event-Driven با Acknowledgment (غیرهمزمان با تأیید دریافت، Timeout و Retry)** | Downstream → Upstream | واگذاری گام Agent Task از طریق رویداد `AgentTaskRequested` با مکانیزم تأیید دریافت (Acknowledgment)؛ در صورت عدم دریافت تأیید در مدت زمان مشخص (Timeout)، رویداد مجدداً ارسال می‌شود (Retry). دریافت نتیجه از طریق رویداد `AgentTaskCompleted`/`AgentTaskFailed`. فراخوانی همزمان مستقیم از این دامنه به Agent Orchestration Engine مجاز نیست (رفع Cycle 3، طبق `Domain_Dependency_Map` بخش ۶.۱). |
| **دانش و زمینه** | **مدیریت دانش** | **Conformist** | Downstream → Upstream | پرس‌وجوی گراف دانش، مدیریت نسخه و Fallback |
| **دانش و زمینه** | **مهندسی داده** | **Conformist** | Downstream → Upstream | داده‌های نمایه‌شده، فراداده |
| **دانش و زمینه** | **مدیریت دانش** | **Shared Kernel** | دوسویه | مدل‌های دانش، موجودیت‌ها (طرحواره گراف دانش) |
| **حافظه و تجربه** | **مدیریت دانش** | **Conformist** | Downstream → Upstream | الگوهای تجربی، حقایق |
| **مدیریت مدل‌ها** | **یادگیری ماشین** | **Conformist** | Downstream → Upstream | مدل‌های ثبت‌شده، نسخه‌ها |

---

### ۶.۳. لایه داده و هوش مصنوعی

| دامنه مبدأ | دامنه مقصد | الگوی تعامل | جهت وابستگی | قراردادهای کلیدی |
| :--- | :--- | :--- | :--- | :--- |
| **مهندسی داده** | **زیرساخت و عملیات** | **Conformist** | Downstream → Upstream | منابع ذخیره‌سازی، Cluster |
| **مهندسی داده** | **هویت و دسترسی** | **Conformist** | Downstream → Upstream | کنترل دسترسی به داده |
| **مهندسی داده** | **مشاهده‌پذیری** | **Conformist** | Downstream → Upstream | Metrics خطوط لوله |
| **یادگیری ماشین** | **مهندسی داده** | **Open Host Service** | Downstream → Upstream | داده‌های آموزشی، ویژگی‌ها |
| **یادگیری ماشین** | **زیرساخت و عملیات** | **Conformist** | Downstream → Upstream | منابع GPU/CPU |
| **مدیریت دانش** | **مهندسی داده** | **Open Host Service** | Downstream → Upstream | هستان‌شناسی، روابط |
| **مدیریت ویژگی‌ها** | **مهندسی داده** | **Conformist** | Downstream → Upstream | داده‌های خام و پردازش‌شده برای محاسبهٔ ویژگی |
| **یادگیری ماشین** | **مدیریت ویژگی‌ها** | **Open Host Service** | Downstream → Upstream | دریافت ویژگی برای آموزش (Batch) و استنتاج (Online) |
| **مهندسی داده** | **مدیریت فراداده و دودمان** | **Conformist** | Downstream → Upstream | ثبت Schema خطوط لوله و ارسال Lineage پردازش |
| **رویداد و Webhook** | **مدیریت فراداده و دودمان** | **Conformist** | Downstream → Upstream | ثبت Schema رویدادها و اعتبارسنجی نسخه‌ها |
| **حاکمیت و انطباق** | **مدیریت فراداده و دودمان** | **Open Host Service** | Downstream → Upstream | دریافت Lineage و Metadata برای ممیزی |

---

### ۶.۴. لایه زیرساخت و حاکمیت

| دامنه مبدأ | دامنه مقصد | الگوی تعامل | جهت وابستگی | قراردادهای کلیدی |
| :--- | :--- | :--- | :--- | :--- |
| **هویت و دسترسی** | **امنیت و حریم خصوصی** | **Shared Kernel** | دوسویه (از طریق هسته مشترک) | مدل هویت و سیاست امنیتی (`User`, `Role`, `Permission`, `Policy`, `Rule`) که در `Data_Model_And_Knowledge_Schema_Overview` (بخش ۶) تعریف شده است. این Shared Kernel وابستگی چرخه‌ای بین دو دامنه را برطرف می‌کند. |
| **هویت و دسترسی** | **حاکمیت و انطباق** | **Conformist** | Downstream → Upstream | سیاست‌های دسترسی، نقش‌ها |
| **امنیت و حریم خصوصی** | **حاکمیت و انطباق** | **Conformist** | Downstream → Upstream | سیاست‌های امنیتی، استانداردها |
| **حاکمیت و انطباق** | **مشاهده‌پذیری** | **Conformist** | Downstream → Upstream | لاگ‌های ممیزی، گزارش‌ها |
| **زیرساخت و عملیات** | **مشاهده‌پذیری** | **Conformist** | Downstream → Upstream | Metrics سلامت، Logs |
| **زیرساخت و عملیات** | **هوش مصنوعی مسئولانه** | **Conformist** | Downstream → Upstream | محدودیت‌های اجرایی، منابع |
| **هوش مصنوعی مسئولانه** | **حاکمیت و انطباق** | **Conformist** | Downstream → Upstream | سیاست‌های Responsible AI |
| **درخواست‌های موضوع داده** | **همه دامنه‌ها** | **Open Host Service** | Downstream → Upstream (برای اجرای عملیات حذف) | قرارداد استاندارد Endpoint حذف (بخش ۱۲.۶ سند `Data_Governance_and_Compliance`) |

---

## ۷. قراردادهای کلیدی بین دامنه‌ها

### ۷.۱. قراردادهای داده‌ای

| قرارداد | دامنه مبدأ | دامنه مقصد | محتوا | فرمت |
| :--- | :--- | :--- | :--- | :--- |
| **درخواست جستجوی دانش** | عامل‌ها و هماهنگی | دانش و زمینه | پرس‌وجو، پارامترها، فیلترها | JSON / Protobuf |
| **پاسخ جستجوی دانش** | دانش و زمینه | عامل‌ها و هماهنگی | نتایج، امتیازات، منابع | JSON / Protobuf |
| **Context Assembly** | دانش و زمینه | عامل‌ها و هماهنگی | Context غنی‌شده، Metadata | JSON / Protobuf |
| **درخواست اجرای Tool** | عامل‌ها و هماهنگی | ابزارها و اقدامات | نام Tool، پارامترها، Context | JSON / Protobuf |
| **نتیجه اجرای Tool** | ابزارها و اقدامات | عامل‌ها و هماهنگی | وضعیت، خروجی، خطا | JSON / Protobuf |
| **درخواست مدل** | عامل‌ها و هماهنگی | مدیریت مدل‌ها | Prompt، پارامترها، محدودیت‌ها | JSON / Protobuf |
| **پاسخ مدل** | مدیریت مدل‌ها | عامل‌ها و هماهنگی | خروجی، Token مصرفی، هزینه | JSON / Protobuf |
| **ذخیره Memory** | عامل‌ها و هماهنگی | حافظه و تجربه | نوع Memory، داده، Metadata | JSON / Protobuf |
| **بازیابی Memory** | عامل‌ها و هماهنگی | حافظه و تجربه | شناسه، نوع، فیلترها | JSON / Protobuf |
| **آغاز اجرای Workflow** | عامل‌ها و هماهنگی | مدیریت جریان کار | شناسه Workflow، ورودی‌ها، Context | JSON (REST همزمان) |
| **درخواست اجرای Agent Task** | مدیریت جریان کار | عامل‌ها و هماهنگی | شناسه Task، پارامترها، Context Reference، مدت Timeout برای Acknowledgment | JSON (Event: `AgentTaskRequested`) |
| **تأیید دریافت Agent Task** | عامل‌ها و هماهنگی | مدیریت جریان کار | شناسه Task، شناسه همبستگی، زمان تأیید | JSON (Event: `AgentTaskAcknowledged`) |
| **نتیجه اجرای Agent Task** | عامل‌ها و هماهنگی | مدیریت جریان کار | شناسه Task، نتیجه، خطا | JSON (Event: `AgentTaskCompleted` / `AgentTaskFailed`) |
| **احراز هویت** | همه دامنه‌ها | هویت و دسترسی | اعتبارنامه، توکن | JWT / OAuth2 |
| **ثبت/دریافت Schema** | مهندسی داده / رویداد و Webhook | مدیریت فراداده و دودمان | نام Schema، نسخه، محتوای Schema (JSON/Avro) | REST / gRPC |
| **اعتبارسنجی Compatibility** | رویداد و Webhook | مدیریت فراداده و دودمان | شناسه Schema، نسخهٔ جدید | REST / gRPC |
| **درخواست ویژگی (Online)** | یادگیری ماشین | مدیریت ویژگی‌ها | نام ویژگی‌ها، شناسه موجودیت | REST / gRPC |
| **درخواست ویژگی (Batch)** | یادگیری ماشین | مدیریت ویژگی‌ها | نام ویژگی‌ها، بازه زمانی | Parquet / Spark |
| **ارسال دادهٔ خام** | مهندسی داده | مدیریت ویژگی‌ها | داده‌های جدول، Streaming | Kafka / Event |

---

### ۷.۲. رویدادهای کلیدی (Event Contracts)

| رویداد | دامنه مبدأ | دامنه مقصد | معنا |
| :--- | :--- | :--- | :--- |
| **DocumentIndexed** | دانش و زمینه | عامل‌ها و هماهنگی | یک سند با موفقیت نمایه شد |
| **KnowledgeUpdated** | دانش و زمینه | عامل‌ها و هماهنگی | دانش به‌روزرسانی شد |
| **ToolCompleted** | ابزارها و اقدامات | عامل‌ها و هماهنگی | اجرای Tool با موفقیت پایان یافت |
| **ToolFailed** | ابزارها و اقدامات | عامل‌ها و هماهنگی | اجرای Tool با خطا مواجه شد |
| **AgentTaskRequested** | مدیریت جریان کار | عامل‌ها و هماهنگی | یک گام Workflow از نوع Agent Task آماده واگذاری به Agent است (همراه با مکانیزم Acknowledgment) |
| **AgentTaskAcknowledged** | عامل‌ها و هماهنگی | مدیریت جریان کار | Agent Orchestration Engine تأیید دریافت Task را ارسال کرد |
| **AgentTaskCompleted** | عامل‌ها و هماهنگی | مدیریت جریان کار | اجرای Agent Task واگذارشده با موفقیت پایان یافت |
| **AgentTaskFailed** | عامل‌ها و هماهنگی | مدیریت جریان کار | اجرای Agent Task واگذارشده با خطا مواجه شد |
| **AgentTaskTimeout** | عامل‌ها و هماهنگی | مدیریت جریان کار | عدم دریافت تأیید یا نتیجه Task در مدت زمان مشخص (Timeout) |
| **AgentFinished** | عامل‌ها و هماهنگی | حافظه و تجربه | چرخه Agent به پایان رسید |
| **MemoryUpdated** | حافظه و تجربه | عامل‌ها و هماهنگی | Memory به‌روزرسانی شد |
| **SecurityAlert** | امنیت و حریم خصوصی | حاکمیت و انطباق | رویداد امنیتی شناسایی شد |
| **PolicyChanged** | حاکمیت و انطباق | همه دامنه‌ها | تغییر در Policyها |
| **ModelDeployed** | مدیریت مدل‌ها | یادگیری ماشین | مدل جدید استقرار یافت |
| **ModelRetired** | مدیریت مدل‌ها | یادگیری ماشین | مدل بازنشسته شد |

---

### ۷.۳. زبان مشترک (Published Language)

برای ارتباط بین دامنه‌ها، از زبان مشترک زیر استفاده می‌شود:

| مفهوم | تعریف | دامنه مالک |
| :--- | :--- | :--- |
| **User** | هویت کاربر نهایی | هویت و دسترسی (Shared Kernel با امنیت) |
| **Role** | نقش سازمانی | هویت و دسترسی (Shared Kernel با امنیت) |
| **Permission** | مجوز دسترسی | هویت و دسترسی (Shared Kernel با امنیت) |
| **Policy** | قانون یا محدودیت قابل اعمال | امنیت و حریم خصوصی / حاکمیت و انطباق (Shared Kernel با هویت) |
| **Agent** | واحد اجرایی هوشمند | عامل‌ها و هماهنگی |
| **Knowledge** | واحد دانش قابل استفاده | دانش و زمینه |
| **Context** | مجموعه اطلاعات مرتبط برای مدل | دانش و زمینه |
| **Memory** | اطلاعات ذخیره‌شده از تعاملات | حافظه و تجربه |
| **Tool** | ابزار قابل اجرا در سیستم خارجی | ابزارها و اقدامات |
| **Task** | واحد کار قابل انجام توسط Agent | عامل‌ها و هماهنگی |
| **Workflow** | دنباله‌ای از گام‌های از پیش تعریف‌شده | مدیریت جریان کار |
| **Model** | مدل هوش مصنوعی قابل استفاده | مدیریت مدل‌ها |
| **Data Subject Request** | درخواست حقوقی کاربر (حذف، اصلاح، حمل داده) | درخواست‌های موضوع داده |

---

## ۸. نگاشت به مؤلفه‌های معماری

هر دامنه به یک یا چند مؤلفه معماری (Component) نگاشت می‌شود. این نگاشت در سند `Component_Map_And_Responsibilities` به‌طور کامل تشریح شده است. خلاصه نگاشت به‌صورت زیر است. برای نگاشت دقیق‌تر هر دامنه به واحد استقرار مستقل (Deployable Service) و دلیل فنی هر تصمیم، به سند `Domain_to_Component_and_Deployment_Mapping` مراجعه کنید.

| دامنه | مؤلفه معماری مرتبط |
| :--- | :--- |
| یکپارچه‌سازی و API | Interface & Integration Layer |
| رویداد و Webhook | Interface & Integration Layer |
| اتصال‌دهنده‌ها | Interface & Integration Layer |
| عامل‌ها و هماهنگی | Agent Orchestration Engine |
| دانش و زمینه | Knowledge & Context Engine |
| حافظه و تجربه | Memory Management Engine |
| ابزارها و اقدامات | Action & Tool Engine |
| مدیریت مدل‌ها | Model Management Engine |
| مدیریت جریان کار | Action & Tool Engine |
| مهندسی داده | Data Layer (زیرساخت) |
| یادگیری ماشین | Model Management Engine |
| مدیریت دانش | Knowledge & Context Engine |
| هویت و دسترسی | Governance & Platform Services |
| امنیت و حریم خصوصی | Governance & Platform Services |
| حاکمیت و انطباق | Governance & Platform Services |
| زیرساخت و عملیات | زیرساخت (Kubernetes، پایگاه‌ها) |
| مشاهده‌پذیری | Governance & Platform Services |
| هوش مصنوعی مسئولانه | Governance & Platform Services |
| درخواست‌های موضوع داده | Governance & Platform Services |

---

## ۹. ارتباط با سایر اسناد (References)

| نام سند | نوع ارتباط |
| :--- | :--- |
| `Domain Landscape` | مرجع دامنه‌های اصلی که این سند نقشه ارتباطات آن‌ها را ترسیم می‌کند |
| `Architecture_Overview_Enterprise_AI_Platform` | مرجع Componentها و لایه‌های معماری که دامنه‌ها بر روی آن‌ها نگاشت می‌شوند |
| `Component_Map_And_Responsibilities` | مرجع مؤلفه‌ها و مسئولیت‌ها که نگاشت دامنه‌ها به مؤلفه‌ها در بخش ۸ بر اساس آن است |
| `API_Contract_Overview` | مرجع قراردادهای API که قراردادهای کلیدی بخش ۷ با آن هم‌راستا هستند |
| `Integration_Boundaries_And_Tooling_Framework` | مرجع الگوهای یکپارچه‌سازی که الگوهای تعامل این سند بر اساس آن‌ها طراحی شده‌اند |
| `Multi_Agent_Collaboration_Model` | مرجع همکاری چند-Agent که رویدادهای مرتبط با Agentها در بخش ۷.۲ بر اساس آن تعریف شده‌اند |
| `Data_Model_And_Knowledge_Schema_Overview` | مرجع مدل داده و Shared Kernel برای هویت/امنیت (بخش ۶) که در بخش ۶.۴ به آن اشاره شده است |
| `Security_and_Privacy_Architecture` | مرجع کنترل‌های امنیتی که رویدادهای امنیتی بخش ۷.۲ با آن هم‌راستا هستند |
| `Domain_Dependency_Map` | مرجع کامل تحلیل و رفع چرخه وابستگی عامل‌ها/مدیریت جریان کار (Cycle 3، بخش ۶.۱ آن سند) که الگوی تعامل بخش ۶.۲ همین سند پیاده‌سازی آن است |
| `Event_&_Webhook_Domain_Architecture` | مرجع مکانیزم Acknowledgment برای رویدادها که در بخش ۶.۲ (مدیریت جریان کار → عامل‌ها) به آن اشاره شده است |

---

## ۱۰. نتیجه‌گیری

این سند نقشه کانتکست دامنه‌های پلتفرم را ترسیم می‌کند و شفافیت کاملی در مورد مرزها، ارتباطات، الگوهای تعامل و قراردادهای بین دامنه‌ها فراهم می‌آورد. با رعایت اصول و الگوهای تعریف‌شده در این سند، تیم‌های توسعه می‌توانند دامنه‌ها را به‌صورت مستقل تکامل دهند، از نشت مفاهیم بین دامنه‌ها جلوگیری کنند و تعاملات بین آن‌ها را به‌صورت استاندارد و قابل اطمینان مدیریت کنند. اصلاحات اعمال‌شده در این نسخه (استفاده از Shared Kernel برای هویت/امنیت و مکانیزم Acknowledgment برای رویدادهای Workflow/Agent) وابستگی‌های چرخه‌ای را برطرف کرده و قابلیت اطمینان ارتباطات بین دامنه‌ای را افزایش می‌دهد.

نقشه کانتکست به‌عنوان **مرجع اصلی برای طراحی رابط‌های بین دامنه‌ها** عمل می‌کند و تضمین می‌کند که تمامی تعاملات بین دامنه‌ها با قراردادهای مشخص، شفاف و قابل رهگیری انجام می‌شود. این امر به کاهش پیچیدگی، افزایش سرعت توسعه و بهبود نگهداری بلندمدت سیستم کمک می‌کند.

**مسئولیت به‌روزرسانی:** تیم معماری به همراه تیم‌های محصول و توسعه مسئول بازبینی دوره‌ای این سند، به‌روزرسانی نقشه ارتباطات بر اساس تغییرات در دامنه‌ها، اضافه شدن دامنه‌های جدید یا تغییر در الگوهای تعامل هستند.

**ثبات سند:** این سند باید با تغییرات در دامنه‌ها، معماری کلان و نیازمندی‌های یکپارچه‌سازی هم‌گام باشد. هر تغییر در ارتباطات بین دامنه‌ها باید در این سند منعکس شود تا سایر اسناد معماری و طراحی نیز به‌روزرسانی شوند.

</div>