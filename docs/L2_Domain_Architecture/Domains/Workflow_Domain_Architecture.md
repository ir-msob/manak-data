<div dir="rtl">

# معماری دامنه مدیریت جریان کار (Workflow Domain Architecture)

**نام پلتفرم:** Enterprise AI Context & Automation Platform

**نسخه:** 1.2

**وضعیت:** بازبینی‌شده

**سند مرجع نام‌گذاری:** `Naming_And_Terminology_Glossary`

---

## ۱. هدف سند

این سند با هدف تعریف معماری دامنه **مدیریت جریان کار** (Workflow Domain) تدوین شده است. این دامنه مسئول مدیریت فرآیندهای چندمرحله‌ای و گردش‌کارهای سازمانی (Business Workflows) است که شامل توالی‌های از پیش تعریف‌شده از گام‌ها، تصمیم‌گیری‌های شرطی، اجرای موازی، انتظار برای رویدادها و تعامل با انسان‌ها (Human‑in‑the‑Loop) می‌شود. هدف آن ایجاد بستری استاندارد، انعطاف‌پذیر، مقیاس‌پذیر و قابل اعتماد برای تعریف، اجرا، پایش و مدیریت گردش‌کارهای پیچیده سازمانی است.

این سند به‌عنوان مرجع اصلی برای تیم‌های توسعه، معماری، محصول و عملیات در زمینه طراحی و پیاده‌سازی زیرساخت گردش‌کارهای سازمانی پلتفرم عمل می‌کند.

---

## ۲. دامنه سند

این سند تمام جنبه‌های مرتبط با دامنه مدیریت جریان کار را پوشش می‌دهد و شامل موارد زیر است:

- **Workflow Definition:** تعریف و ثبت گردش‌کارها با استفاده از زبان‌های استاندارد (BPMN، DSL، JSON)
- **Workflow Engine:** موتور اجرای گردش‌کارها با پشتیبانی از اجرای ترتیبی، موازی، شرطی و مبتنی بر رویداد
- **State Manager:** مدیریت وضعیت هر گردش‌کار و گام‌های آن در طول اجرا، با پشتیبانی از Snapshotting و Event Logging
- **Task Management:** مدیریت وظایف (Task) درون هر گردش‌کار شامل ایجاد، تخصیص، اجرا و تکمیل
- **Human‑in‑the‑Loop:** مدیریت گام‌هایی که نیاز به تأیید، ورودی یا تصمیم‌گیری انسانی دارند
- **Workflow Orchestration:** هماهنگی با عامل‌ها و ابزارها برای اجرای گام‌ها، با مکانیزم تأیید دریافت (Acknowledgment) برای گام‌های واگذارشده به Agent
- **Error Handling & Recovery:** مدیریت خطا، Retry (مطابق با سیاست یکپارچه)، Rollback و اقدامات جبرانی (Orchestrated Saga) در گردش‌کارهای چندمرحله‌ای
- **Workflow Monitoring & Observability:** پایش وضعیت، پیشرفت و سلامت گردش‌کارها
- **Workflow Versioning:** نسخه‌بندی گردش‌کارها برای مدیریت تغییرات و سازگاری با گردش‌کارهای در حال اجرا
- **Workflow Templates:** قالب‌های از پیش تعریف‌شده برای گردش‌کارهای رایج سازمانی
- **Workflow Instance Management:** ایجاد، مدیریت و پایش نمونه‌های اجرای گردش‌کار (Workflow Instance)
- **Workflow Trigger Management:** مدیریت روش‌های آغاز گردش‌کار شامل API، رویداد، زمان‌بندی، Webhook و اجرای دستی
- **Workflow Scheduling:** زمان‌بندی اجرای گردش‌کارها و پشتیبانی از اجرای دوره‌ای (Cron)
- **Workflow Validation:** اعتبارسنجی تعریف گردش‌کار قبل از انتشار و اجرا
- **State Recovery:** بازیابی گردش‌کارهای در حال اجرا از آخرین Snapshot در صورت خرابی State Manager

جزئیات پیاده‌سازی فنی و کدنویسی در اسناد سطح پایین‌تر تدوین می‌شوند. این سند صرفاً **معماری سطح بالا و اصول طراحی** دامنه را تعریف می‌کند.

---

## ۳. اصول طراحی دامنه

| اصل | توضیح |
| :--- | :--- |
| **انعطاف‌پذیری (Flexibility)** | گردش‌کارها باید به‌صورت پویا و با استفاده از زبان‌های استاندارد (BPMN، DSL) تعریف شوند و امکان تغییر در زمان اجرا (Runtime) را داشته باشند. |
| **قابلیت رهگیری (Traceability)** | تمام گام‌های هر گردش‌کار، تغییرات وضعیت، تصمیمات و نتایج باید ثبت و قابل ردیابی باشند تا ممیزی و تحلیل امکان‌پذیر باشد. |
| **مقاومت در برابر خطا (Fault Tolerance)** | گردش‌کارها باید در برابر خطاهای گام‌ها، قطعی سرویس‌ها و Timeout مقاوم باشند و با مکانیزم‌های Retry، Rollback و Compensation مدیریت شوند. |
| **مدیریت وضعیت (State Management)** | وضعیت هر گردش‌کار و گام‌های آن باید به‌صورت پایدار (Persistent) ذخیره شود تا در صورت خرابی سیستم، گردش‌کار از همان نقطه ادامه یابد. |
| **هماهنگی با عامل‌ها و ابزارها** | گردش‌کارها باید بتوانند گام‌های خود را به عامل‌های هوشمند یا ابزارهای اجرایی واگذار کنند و نتایج را دریافت کنند. |
| **حضور انسان در حلقه (Human‑in‑the‑Loop)** | گردش‌کارها باید از گام‌هایی پشتیبانی کنند که نیاز به تأیید، ورودی یا تصمیم‌گیری انسانی دارند و مکانیزم انتظار و Timeout برای آن‌ها تعریف شود. |
| **نسخه‌بندی (Versioning)** | گردش‌کارها باید نسخه‌بندی شوند تا تغییرات در تعریف گردش‌کار، گردش‌کارهای در حال اجرا را مختل نکند. تعریف پایه Semantic Versioning در سند `Versioning_And_Compatibility_Strategy` آمده است. |
| **مقیاس‌پذیری (Scalability)** | موتور گردش‌کار باید توانایی مدیریت هزاران گردش‌کار همزمان با گام‌های متعدد را داشته باشد. |
| **عدم اجرای تکراری (Idempotency)** | اجرای مجدد یک درخواست، پیام یا رویداد نباید باعث اجرای چندباره یک گام یا ایجاد داده تکراری شود. |
| **جداسازی مستأجران (Multi-Tenancy Isolation)** | تمام داده‌ها، وضعیت‌ها، رویدادها و منابع هر مستأجر باید به‌صورت کامل از سایر مستأجران ایزوله باشند. |
| **یکپارچگی با سیاست Retry و Timeout** | تمام مکانیزم‌های Retry و Timeout در گردش‌کارها باید از سیاست یکپارچه تعریف‌شده در `Non_Functional_Requirements_And_SLA` (بخش ۶.۱ و ۶.۲) پیروی کنند. |

---

## ۴. مؤلفه‌های اصلی دامنه

### ۴.۱. Workflow Definition (تعریف گردش‌کار)

**مسئولیت:** تعریف و ثبت گردش‌کارها با استفاده از زبان‌های استاندارد و قابل فهم برای انسان و ماشین.

**وظایف کلیدی:**

- **ثبت گردش‌کار:** ثبت گردش‌کار جدید با فراداده کامل (نام، توضیحات، نسخه، مالک)
- **تعریف گام‌ها (Steps):** تعریف گام‌های گردش‌کار شامل نوع (Task، Decision، Parallel، Wait، Human)
- **تعریف وابستگی‌ها:** تعیین وابستگی‌های بین گام‌ها (ترتیبی، موازی، شرطی)
- **تعریف شرط‌ها (Conditions):** تعریف شرایط تصمیم‌گیری و انشعاب در گردش‌کار
- **تعریف ورودی/خروجی:** تعیین داده‌های ورودی به هر گام و خروجی مورد انتظار
- **تعریف خطا و Compensation:** تعریف رفتار در صورت خطا و اقدامات جبرانی (مطابق با `Transaction_And_Compensation_Strategy`)

**زبان‌های تعریف گردش‌کار:**

| زبان | توضیح | کاربرد |
| :--- | :--- | :--- |
| **BPMN 2.0** | استاندارد جهانی برای مدل‌سازی فرآیندهای کسب‌وکار | گردش‌کارهای پیچیده سازمانی |
| **DSL (Domain Specific Language)** | زبان اختصاصی دامنه مبتنی بر JSON/YAML | گردش‌کارهای ساده تا متوسط |
| **Workflow DSL** | زبان اختصاصی پلتفرم برای تعریف گردش‌کارها | گردش‌کارهایی که با عامل‌ها و ابزارها تعامل دارند |
| **State Machine** | مدل مبتنی بر ماشین حالت برای گردش‌کارهای با حالت محدود | گردش‌کارهای ساده و قابل پیش‌بینی |

**نمونه تعریف گردش‌کار به‌صورت DSL:**

```json
{
  "workflow_id": "wf-123",
  "name": "تأیید و انتشار مستندات",
  "version": "1.0",
  "steps": [
    {
      "id": "step-1",
      "type": "task",
      "name": "بارگذاری سند",
      "action": "document_upload",
      "next": "step-2",
      "on_failure": {
        "retry_count": 2,
        "retry_delay_seconds": 5,
        "fallback_action": "abort"
      }
    },
    {
      "id": "step-2",
      "type": "human",
      "name": "تأیید محتوا توسط مدیر",
      "assignee": "manager",
      "timeout": "24h",
      "next_condition": {
        "approved": "step-3",
        "rejected": "step-4"
      },
      "on_failure": {
        "retry_count": 0,
        "fallback_action": "compensate"
      }
    },
    {
      "id": "step-3",
      "type": "task",
      "name": "انتشار سند",
      "action": "document_publish",
      "next": "step-5",
      "on_failure": {
        "retry_count": 1,
        "retry_delay_seconds": 10,
        "fallback_action": "compensate"
      }
    },
    {
      "id": "step-4",
      "type": "task",
      "name": "ارسال بازخورد به نویسنده",
      "action": "send_feedback",
      "next": "step-5",
      "on_failure": {
        "retry_count": 3,
        "retry_delay_seconds": 5,
        "fallback_action": "compensate"
      }
    },
    {
      "id": "step-5",
      "type": "end",
      "name": "پایان گردش‌کار"
    }
  ],
  "on_failure": {
    "global_compensation": true,
    "compensation_timeout_seconds": 300,
    "compensation_retry_policy": {
      "max_retries": 3,
      "backoff_seconds": 30
    }
  }
}
```

---

#### ۴.۱.۱. Workflow Instance (نمونه اجرای گردش‌کار)

**مسئولیت:** ایجاد و مدیریت نمونه‌های اجرای گردش‌کار بر اساس یک تعریف (Workflow Definition).

**وظایف کلیدی:**

- ایجاد نمونه جدید از گردش‌کار
- نگهداری وضعیت اجرای هر نمونه
- ارتباط هر نمونه با نسخه مشخصی از تعریف گردش‌کار
- مدیریت متغیرها و داده‌های اختصاصی هر نمونه
- مدیریت شناسه کسب‌وکاری (Business Key) برای رهگیری فرآیندهای سازمانی

---

#### ۴.۱.۲. Workflow پیش‌فرض: حذف داده‌های کاربر (Data Deletion Workflow)

این Workflow به‌عنوان یک Workflow سیستمی و از پیش تعریف‌شده در پلتفرم وجود دارد و توسط سرویس `Data Subject Request Service` فراخوانی می‌شود.

```json
{
  "workflow_id": "wf-deletion",
  "name": "حذف کامل داده‌های کاربر",
  "version": "1.0",
  "description": "این Workflow تمام داده‌های یک کاربر را از تمام لایه‌ها حذف یا ناشناس می‌کند.",
  "steps": [
    {
      "id": "step-1",
      "type": "task",
      "name": "حذف Session Memory",
      "action": "memory.delete_session_memory",
      "next": "step-2"
    },
    {
      "id": "step-2",
      "type": "task",
      "name": "حذف Long-Term Memory",
      "action": "memory.delete_long_term_memory",
      "next": "step-3"
    },
    {
      "id": "step-3",
      "type": "task",
      "name": "حذف Knowledge Base",
      "action": "knowledge.delete_user_knowledge",
      "next": "step-4"
    },
    {
      "id": "step-4",
      "type": "task",
      "name": "حذف Knowledge Graph",
      "action": "knowledge_graph.delete_user_entities",
      "next": "step-5"
    },
    {
      "id": "step-5",
      "type": "task",
      "name": "ناشناس‌سازی Audit Logs",
      "action": "audit.anonymize_user_logs",
      "next": "step-6"
    },
    {
      "id": "step-6",
      "type": "task",
      "name": "ناشناس‌سازی Application Logs",
      "action": "logging.anonymize_user_logs",
      "next": "step-7"
    },
    {
      "id": "step-7",
      "type": "task",
      "name": "حذف Feature Store",
      "action": "feature.delete_user_features",
      "next": "step-8"
    },
    {
      "id": "step-8",
      "type": "task",
      "name": "حذف Tool Execution History",
      "action": "tool.delete_user_history",
      "next": "step-9"
    },
    {
      "id": "step-9",
      "type": "end",
      "name": "پایان فرایند حذف"
    }
  ],
  "on_failure": {
    "global_compensation": false,
    "alert_ops": true,
    "fallback_status": "Failed"
  }
}
```

---

### ۴.۲. Workflow Engine (موتور گردش‌کار)

**مسئولیت:** اجرای گردش‌کارها، مدیریت گام‌ها، وضعیت‌ها و هماهنگی با سایر سرویس‌ها.

**وظایف کلیدی:**

- **شروع گردش‌کار:** راه‌اندازی یک نمونه جدید از گردش‌کار با داده‌های ورودی
- **مدیریت گام‌ها:** اجرای گام‌ها بر اساس ترتیب، شرایط و وابستگی‌ها
- **انشعاب و شرط:** مدیریت انشعاب‌ها و تصمیم‌گیری‌های شرطی در گردش‌کار
- **اجرای موازی:** اجرای گام‌های مستقل به‌صورت هم‌زمان
- **انتظار و Timeout:** مدیریت گام‌های انتظار (برای رویداد یا تأیید انسانی) و Timeout
- **هماهنگی با عامل‌ها:** ارسال درخواست به عامل‌ها برای اجرای گام‌های هوشمند با مکانیزم Acknowledgment
- **هماهنگی با ابزارها:** فراخوانی ابزارها برای اجرای گام‌های عملیاتی
- **مدیریت خطا:** مدیریت خطاهای گام‌ها با Retry (مطابق با `Non_Functional_Requirements_And_SLA`)، Rollback و اقدامات جبرانی (مطابق با `Transaction_And_Compensation_Strategy`)
- **اجرای Sub Workflow:** اجرای گردش‌کارهای دیگر به‌عنوان بخشی از یک گردش‌کار بزرگ‌تر
- **Correlation رویدادها:** تطبیق رویدادهای دریافتی با نمونه صحیح گردش‌کار بر اساس شناسه‌های کسب‌وکاری
- **اجرای Idempotent:** جلوگیری از اجرای مجدد گام‌ها در صورت دریافت درخواست یا رویداد تکراری

---

### ۴.۳. State Manager (مدیر وضعیت) - با قابلیت Event Sourcing و Snapshotting

**مسئولیت:** مدیریت پایدار و قابل اعتماد وضعیت هر گردش‌کار و گام‌های آن، به‌همراه ذخیره‌سازی تاریخچه کامل رویدادهای اجرا برای پشتیبانی از عملیات جبران و بازگشت.

**وظایف کلیدی:**

- **ذخیره‌سازی وضعیت جاری:** وضعیت فعلی گردش‌کار (Running, Paused, Completed, Failed, Compensating, Compensated, COMPENSATION_FAILED, WAITING_FOR_AGENT_ACK) و هر گام.
- **ذخیره‌سازی تاریخچه رویدادها (Event Log):** ثبت ترتیبی تمام رویدادهای مهم در طول اجرا، مانند:
    - `StepStarted(step_id, timestamp)`
    - `StepCompleted(step_id, result, timestamp)`
    - `StepFailed(step_id, error, timestamp)`
    - `StepCompensated(step_id, timestamp)`
    - `CompensationFailed(step_id, error, timestamp)`
- **Snapshotting:** به‌صورت دوره‌ای (هر ۵ دقیقه یا پس از هر گام حیاتی)، یک Snapshot از وضعیت کامل گردش‌کار (شامل متغیرها و وضعیت گام‌ها) در State Manager ذخیره شود تا زمان بازیابی کاهش یابد.
- **بازیابی وضعیت:** امکان بازسازی وضعیت جاری و لیست گام‌های موفق از روی تاریخچه رویدادها و آخرین Snapshot، برای ادامه اجرا یا شروع جبران.
- **مدیریت قفل:** جلوگیری از اجرای هم‌زمان یک گردش‌کار توسط چندین نمونه.

**فیلدهای کلیدی وضعیت گردش‌کار (به‌روزرسانی‌شده):**

| فیلد | توضیح |
| :--- | :--- |
| `workflow_instance_id` | شناسه یکتای نمونه گردش‌کار |
| `workflow_definition_id` | شناسه تعریف گردش‌کار |
| `version` | نسخه تعریف گردش‌کار |
| `status` | وضعیت (Running, Paused, Completed, Failed, Compensating, Compensated, COMPENSATION_FAILED, WAITING_FOR_AGENT_ACK) |
| `current_step_id` | شناسه گام فعلی (در صورت در حال اجرا) |
| `steps` | لیست گام‌ها با وضعیت و داده‌های هرکدام |
| `variables` | متغیرهای سراسری گردش‌کار |
| `started_at` | زمان شروع |
| `completed_at` | زمان پایان (در صورت اتمام) |
| `updated_at` | زمان آخرین به‌روزرسانی |
| `tenant_id` | شناسه مستأجر |
| `created_by` | کاربر یا سیستم شروع‌کننده |
| `business_key` | شناسه کسب‌وکاری جهت تطبیق رویدادها |
| `correlation_id` | شناسه ارتباط بین درخواست‌ها |
| `parent_workflow_instance_id` | شناسه گردش‌کار والد |
| `event_log` | لیست رویدادهای اجرا (JSON array) برای پشتیبانی از جبران |
| `compensation_status` | وضعیت جبران (NotStarted, InProgress, Completed, Failed) |
| `last_snapshot_id` | شناسه آخرین Snapshot ذخیره‌شده |
| `last_snapshot_at` | زمان آخرین Snapshot |

---

### ۴.۴. Task Management (مدیریت وظایف)

**مسئولیت:** مدیریت وظایف (Task) درون هر گردش‌کار شامل ایجاد، تخصیص، اجرا و تکمیل.

**وظایف کلیدی:**

- **ایجاد Task:** ایجاد Task برای هر گام گردش‌کار
- **تخصیص Task:** تخصیص Task به انسان (User) یا عامل (Agent)
- **مدیریت اولویت:** تعیین اولویت Task‌ها برای اجرا
- **مدیریت وضعیت:** Track وضعیت Task (Open, Assigned, In Progress, Completed, Canceled)
- **مدیریت زمان‌بندی:** تعیین زمان‌بندی اجرا یا مهلت انجام
- **یکپارچگی با عامل‌ها:** ارسال Task به عامل‌های هوشمند برای اجرا با مکانیزم Acknowledgment
- **یکپارچگی با انسان:** ارائه Task به کاربران از طریق رابط کاربری برای تأیید یا ورودی

**انواع Task:**

| نوع Task | توضیح | مجری |
| :--- | :--- | :--- |
| **Automated Task** | گامی که به‌طور کامل خودکار توسط سیستم اجرا می‌شود | سیستم / ابزار |
| **Agent Task** | گامی که به یک عامل هوشمند واگذار می‌شود | عامل هوشمند |
| **Human Task** | گامی که نیاز به اقدام، تأیید یا ورودی انسانی دارد | کاربر |
| **Integration Task** | گامی که یکپارچه‌سازی با سیستم خارجی را انجام می‌دهد | ابزار / API |
| **Wait Task** | گامی که برای وقوع یک رویداد یا شرط منتظر می‌ماند | سیستم |
| **Sub Workflow Task** | اجرای یک گردش‌کار دیگر به‌عنوان بخشی از گردش‌کار جاری | موتور گردش‌کار |
| **Script Task** | اجرای منطق یا اسکریپت داخلی | سیستم |
| **Timer Task** | انتظار تا زمان مشخص یا اجرای زمان‌بندی‌شده | سیستم |

---

### ۴.۵. Human‑in‑the‑Loop (حضور انسان در حلقه)

**مسئولیت:** مدیریت گام‌هایی که نیاز به تأیید، ورودی یا تصمیم‌گیری انسانی دارند.

**وظایف کلیدی:**

- **ارسال درخواست به کاربر:** ارسال درخواست تأیید، ورودی یا تصمیم‌گیری به کاربر از طریق رابط کاربری
- **انتظار برای پاسخ:** انتظار برای پاسخ کاربر با امکان Timeout
- **مدیریت Timeout:** در صورت عدم پاسخ کاربر در زمان مشخص، اقدام جایگزین (Escalate، Cancel، Auto‑Approve)
- **ثبت تصمیم:** ثبت تصمیم انسانی برای ممیزی و یادگیری
- **اعمال تصمیم:** اعمال تصمیم کاربر بر روی گردش‌کار (ادامه، اصلاح، لغو)

**سناریوهای Human‑in‑the‑Loop:**

| سناریو | توضیح | مثال |
| :--- | :--- | :--- |
| **Approval** | تأیید یا رد یک گام توسط مدیر | تأیید مستندات، تأیید تغییرات |
| **Information Input** | وارد کردن اطلاعات توسط کاربر | تکمیل فرم، ارائه توضیحات |
| **Decision Making** | تصمیم‌گیری بین چند گزینه توسط کاربر | انتخاب مسیر گردش‌کار، انتخاب رویکرد |
| **Error Escalation** | ارجاع خطا یا استثنا به انسان برای تصمیم‌گیری | خطای غیرقابل‌بازیابی، وضعیت غیرمنتظره |

---

#### ۴.۵.۱. Workflow Trigger Management (مدیریت آغاز گردش‌کار)

**مسئولیت:** مدیریت تمامی روش‌های آغاز اجرای گردش‌کار.

**وظایف کلیدی:**

- آغاز از طریق API
- آغاز از طریق رویداد (Event)
- آغاز زمان‌بندی‌شده (Scheduler)
- آغاز از طریق Webhook
- آغاز دستی توسط کاربر
- آغاز توسط سایر گردش‌کارها

---

### ۴.۵.۲. بازیابی دستی گردش‌کارهای COMPENSATION_FAILED

برای گردش‌کارهایی که به وضعیت `COMPENSATION_FAILED` رسیده‌اند، یک رابط مدیریتی (Admin UI) با قابلیت‌های زیر فراهم می‌شود:

- **مشاهده درخواست‌های DLQ:** نمایش لیست درخواست‌های جبرانی ناموفق با جزئیات کامل (شناسه گردش‌کار، گام، Tool، پیام خطا، تعداد Retry).
- **Retry دستی عملیات جبرانی:** امکان اجرای مجدد عملیات جبرانی برای یک گام خاص.
- **لغو دستی گردش‌کار:** امکان خاتمه گردش‌کار با ثبت دلیل در Audit Log.
- **ادامه گردش‌کار:** در صورت امکان، ادامه گردش‌کار از نقطه شکست (با تأیید اپراتور).

تمامی عملیات انجام‌شده از طریق این رابط، باید در Audit Log با شناسه اپراتور ثبت شوند.

---

### ۴.۶. Error Handling & Recovery (مدیریت خطا و بازیابی) - با پشتیبانی از Orchestrated Saga

**مسئولیت:** مدیریت خطاها در گردش‌کارهای چندمرحله‌ای با استفاده از الگوی **Orchestrated Saga** (طبق سند `Transaction_And_Compensation_Strategy`)، شامل مکانیزم‌های Retry (مطابق با سیاست یکپارچه)، Rollback (از طریق Compensation) و مدیریت شرایط بحرانی.

**وظایف کلیدی:**

- **تشخیص خطا:** شناسایی خطا در هر گام گردش‌کار بر اساس کد خطای بازگشتی از Tool یا Timeout.
- **Retry:** تلاش مجدد با Exponential Backoff و Jitter برای خطاهای موقت، مطابق با سیاست یکپارچه Retry در `Non_Functional_Requirements_And_SLA` (بخش ۶.۱). تعداد Retry، فاصله زمانی و حداکثر زمان کل Retry بر اساس مقادیر پیش‌فرض جهانی یا تنظیمات اختصاصی گام است.
- **شروع جبران (Compensation):** در صورت غیرقابل بازیابی بودن خطا (یا پس از اتمام Retry)، Orchestrator (Workflow Engine) مسئول آغاز فرایند جبران به‌صورت معکوس است.
    - **مراحل جبران:**
        1. تعیین لیست گام‌های موفق قبلی با استفاده از `event_log` و `last_snapshot_id` در State Manager.
        2. پیمایش معکوس لیست و برای هر گام:
            - بررسی `compensable` بودن در Tool Registry.
            - اگر `compensable: true`، فراخوانی `compensation_action` از طریق Execution Engine (در دامنه ابزارها).
            - ثبت نتیجه (موفق یا ناموفق) در `event_log`.
            - در صورت موفقیت، وضعیت گام به `Compensated` تغییر می‌کند.
            - در صورت شکست عملیات جبرانی، به بخش "مدیریت شکست در حین جبران" مراجعه کنید.
        3. پس از اتمام جبران تمام گام‌ها، وضعیت گردش‌کار به `Compensated` تغییر می‌یابد.
- **مدیریت شکست در حین جبران (Failure during Compensation):**
    - اگر عملیات جبرانی یک گام با خطا مواجه شود، Orchestrator نباید بی‌نهایت Retry کند.
    - **راهکار:** عملیات جبرانی را در یک **Dead Letter Queue (DLQ)** قرار داده و یک **Scheduler** مجزا برای Retry با Backoff طولانی‌تر (مثلاً هر ۱ ساعت) در نظر بگیرید. (مطابق با `Transaction_And_Compensation_Strategy`).
    - به‌طور هم‌زمان، یک **هشدار Critical** به تیم عملیات ارسال شود تا به‌صورت دستی وضعیت را بررسی کنند، زیرا داده‌ها در وضعیت نیمه‌جبران‌شده (Semi-compensated) باقی مانده‌اند.
    - گردش‌کار در وضعیت `COMPENSATION_FAILED` قرار می‌گیرد و امکان مداخله دستی (از طریق رابط کاربری مدیریت در بخش ۴.۵.۲) برای ادامه یا لغو دستی فراهم می‌شود.
- **مدیریت Toolهای غیرقابل جبران (NON_COMPENSABLE):**
    - در صورت شکست یک Tool با `compensation_capability = NON_COMPENSABLE`، Orchestrator باید `non_compensable_failure_policy` را اعمال کند (طبق `Tool_Domain_Architecture`):
        - `ABORT_WORKFLOW`: گردش‌کار بدون شروع جبران، با وضعیت `FAILED` پایان می‌یابد و خطا ثبت می‌شود.
        - `CONTINUE_WITH_WARNING`: گردش‌کار با وضعیت `COMPLETED_WITH_WARNINGS` ادامه می‌یابد و خطا در Audit Log ثبت می‌شود.
        - `HUMAN_ESCALATION`: گردش‌کار به وضعیت `WAITING_MANUAL_INTERVENTION` می‌رود و هشدار برای مداخله دستی ارسال می‌شود.
- **ثبت خطا:** ثبت کامل خطاها، تلاش‌های جبران، و نتایج برای تحلیل و بهبود.
- **Idempotency Check:** جلوگیری از اجرای مجدد عملیات در صورت دریافت درخواست یا رویداد تکراری (با استفاده از `correlation_id`).
- **Transaction Boundary:** هر Tool خودش مسئول Atomicity عملیات داخلی خود است. Workflow Engine فقط ترتیب و جبران را در سطح کلان مدیریت می‌کند و از مدل **Eventual Consistency** پیروی می‌کند.

**الگوی Compensation (اقدام جبرانی) - مثال کامل:**

```text
Step 1: Create Issue (Jira)  → موفق
        │
        ▼
Step 2: Assign to Developer  → موفق
        │
        ▼
Step 3: Update Database      → شکست (غیرقابل Retry)
        │
        ▼
شروع جبران (Reverse Saga):
        │
        ├── جبران Step 2: Unassign Issue → موفق
        │
        ├── جبران Step 1: Close/Delete Issue → موفق
        │
        ▼
وضعیت نهایی: Compensated
```

**Dead Letter Queue برای جبران ناموفق:**

```text
اگر جبران Step 2 با خطا مواجه شود:
        │
        ├── قرار دادن درخواست جبران در DLQ
        │
        ├── ارسال هشدار Critical به تیم عملیات
        │
        ├── وضعیت گردش‌کار: COMPENSATION_FAILED (نیازمند مداخله دستی)
        │
        └── Scheduler هر ۱ ساعت Retry از DLQ
```

---

### ۴.۷. بازیابی وضعیت گردش‌کار (State Recovery)

در صورت خرابی **State Manager** یا راه‌اندازی مجدد Workflow Engine، سیستم باید بتواند گردش‌کارهای در حال اجرا را بازیابی کند:

1. **Snapshotting:** به‌صورت دوره‌ای (هر ۵ دقیقه یا پس از هر گام حیاتی)، یک Snapshot از وضعیت کامل گردش‌کار (شامل متغیرها و وضعیت گام‌ها) در State Manager ذخیره شود.
2. **بازیابی خودکار:** در زمان راه‌اندازی مجدد، Workflow Engine باید آخرین Snapshot را برای هر گردش‌کار در حال اجرا بارگذاری کرده و از آخرین گام موفق ادامه دهد. در صورت عدم وجود Snapshot، از `event_log` برای بازسازی وضعیت استفاده می‌شود.
3. **سیاست نگهداری Event Log:** رویدادهای ذخیره‌شده در `event_log` باید حداقل به‌مدت ۷ روز نگهداری شوند تا امکان بازتولید و عیب‌یابی فراهم باشد. رویدادهای قدیمی‌تر باید به Cold Storage منتقل شوند.
4. **High Availability برای State Manager:** State Manager باید با استفاده از پایگاه داده با Replication (حداقل ۳ گره) و Failover خودکار استقرار یابد.

---

### ۴.۸. وضعیت‌های جدید گردش‌کار

وضعیت‌های زیر به وضعیت‌های موجود اضافه می‌شوند:

| وضعیت | توضیح |
| :--- | :--- |
| **WAITING_FOR_AGENT_ACK** | گردش‌کار منتظر تأیید دریافت (Acknowledgment) از Agent Orchestration Engine برای یک Task واگذارشده است. |
| **COMPENSATION_FAILED** | عملیات جبرانی با شکست مواجه شده و نیاز به مداخله دستی دارد. |
| **COMPENSATED** | عملیات جبرانی با موفقیت انجام شده و گردش‌کار به پایان رسیده است (با وضعیت Failed اما جبران‌شده). |

---

### ۴.۹. Workflow Monitoring & Observability (پایش گردش‌کار)

**مسئولیت:** پایش وضعیت، پیشرفت و سلامت گردش‌کارها.

**متریک‌های کلیدی:**

| متریک | توضیح |
| :--- | :--- |
| **Workflow Count** | تعداد گردش‌کارهای فعال، تکمیل‌شده و ناموفق |
| **Step Count** | تعداد گام‌های هر گردش‌کار |
| **Average Duration** | میانگین زمان اجرای گردش‌کارها |
| **Success Rate** | درصد گردش‌کارهای موفق |
| **Failure Rate** | درصد گردش‌کارهای ناموفق با جزئیات نوع خطا |
| **Human Task Duration** | میانگین زمان تکمیل Taskهای انسانی |
| **Retry Count** | تعداد Retry در گام‌های مختلف |
| **Workflow SLA Violation** | تعداد گردش‌کارهایی که از زمان مجاز عبور کرده‌اند |
| **Queue Size** | تعداد گردش‌کارهای در انتظار اجرا |
| **Average Wait Time** | میانگین زمان انتظار در گام‌های انسانی یا رویدادی |
| **Execution Throughput** | تعداد گردش‌کارهای تکمیل‌شده در واحد زمان |
| **Compensation Failure Rate** | درصد عملیات جبرانی ناموفق |

**داشبورد پیشنهادی:**

- **Overall Workflow Health:** وضعیت کلی گردش‌کارها
- **Running Workflows:** لیست گردش‌کارهای در حال اجرا با پیشرفت
- **Failed Workflows:** گردش‌کارهای ناموفق با جزئیات خطا
- **Human Tasks Pending:** Taskهای انسانی در انتظار پاسخ
- **Performance Trends:** روند عملکرد گردش‌کارها در طول زمان
- **Compensation Status Dashboard:** وضعیت گردش‌کارهای در حال جبران یا نیازمند مداخله دستی

---

## ۵. جریان کامل گردش‌کار

جریان کامل از شروع تا پایان یک گردش‌کار:

```text
Trigger (API, Event, Scheduled, Manual)
        │
        ▼
Workflow Instance Created
        │
        ├── Step 1: Execute (Automated / Agent / Human)
        │       │
        │       ├── Success → Continue
        │       │
        │       └── Failure → Retry (مطابق با سیاست یکپارچه) / Rollback / Compensation
        │
        ├── Step 2: Execute
        │       │
        │       └── ...
        │
        ├── Decision Step (Conditional)
        │       │
        │       ├── Condition A → Path A
        │       │
        │       └── Condition B → Path B
        │
        ├── Parallel Execution
        │       │
        │       ├── Task A (Async)
        │       │
        │       ├── Task B (Async)
        │       │
        │       └── Wait for All
        │
        ├── Human Task (Approval / Input)
        │       │
        │       ├── Approved → Continue
        │       │
        │       ├── Rejected → Compensation / End
        │       │
        │       └── Timeout → Escalation / Auto‑Action
        │
        ├── Agent Task (با مکانیزم Acknowledgment)
        │       │
        │       ├── ارسال AgentTaskRequested
        │       │
        │       ├── WAITING_FOR_AGENT_ACK
        │       │
        │       ├── دریافت Acknowledged → ادامه
        │       │
        │       ├── Timeout → Retry / Human Escalation
        │       │
        │       └── دریافت نتیجه → ادامه / خطا
        │
        ▼
Workflow Completed / Failed / Compensated / COMPENSATION_FAILED / Canceled
        │
        ▼
Memory Update (Experience Store)
        │
        ▼
Events Published (WorkflowCompleted / WorkflowFailed / CompensationFailed)
```

---

## ۶. قوانین کسب‌وکار مرتبط

| شناسه | عنوان | شرح | اولویت |
| :--- | :--- | :--- | :--- |
| BR‑066 | حداکثر زمان اجرای گردش‌کار | هر گردش‌کار باید در مدت زمان مشخصی (Timeout) اجرا شود و در غیر این صورت لغو شود. | بالا |
| BR‑067 | Retry برای گام‌های ناموفق | گام‌های ناموفق باید با سیاست Retry مشخص (مطابق با `Non_Functional_Requirements_And_SLA`) تکرار شوند. | بالا |
| BR‑068 | Compensation برای گردش‌کارهای شکست‌خورده | در صورت شکست گردش‌کار، اقدامات جبرانی برای بازگردانی وضعیت اجرا می‌شوند (مطابق با `Transaction_And_Compensation_Strategy`). | بالا |
| BR‑069 | تأیید انسانی برای گام‌های حساس | گام‌های با تأثیر بالا نیاز به تأیید انسانی دارند. | بحرانی |
| BR‑070 | نسخه‌بندی گردش‌کار | تغییر در تعریف گردش‌کار باید با نسخه‌بندی مدیریت شود تا گردش‌کارهای در حال اجرا مختل نشوند. | بالا |
| BR‑071 | ذخیره‌سازی وضعیت گردش‌کار | وضعیت هر گردش‌کار باید به‌صورت پایدار ذخیره شود تا در صورت خرابی سیستم قابل بازیابی باشد. | بالا |
| BR‑072 | محدودیت گردش‌کارهای همزمان | تعداد گردش‌کارهای همزمان برای هر Tenant نباید از حد مشخصی تجاوز کند. | بالا |
| BR‑073 | جلوگیری از اجرای تکراری | هر درخواست یا رویداد تکراری نباید باعث اجرای مجدد یک گام شود. | بحرانی |
| BR‑074 | ایزوله‌سازی مستأجران | داده‌ها و وضعیت گردش‌کار هر مستأجر باید کاملاً از سایر مستأجران مستقل باشد. | بحرانی |
| BR‑075 | حفظ نسخه اجرای گردش‌کار | هر نمونه گردش‌کار باید تا پایان اجرا از همان نسخه تعریف اولیه استفاده کند. | بالا |
| BR‑124 | مکانیزم Acknowledgment برای Agent Task | هر گام از نوع Agent Task باید با مکانیزم تأیید دریافت (Acknowledgment)، Timeout و Retry همراه باشد تا از تحویل قطعی اطمینان حاصل شود. | بالا |
| BR‑125 | رفتار در صورت شکست جبران | در صورت شکست عملیات جبرانی، گردش‌کار باید به وضعیت `COMPENSATION_FAILED` رفته و درخواست به Dead Letter Queue منتقل شود. هشدار Critical به تیم عملیات ارسال می‌شود. | بحرانی |

> **ارجاع:** قوانین کامل در سند `Business_Rule_Catalog` ثبت شده‌اند.

---

## ۷. رویدادهای دامنه (Domain Events)

| شناسه | نام رویداد | نسخه | دامنه تولیدکننده | دامنه‌های مصرف‌کننده | محرک | معنای کسب‌وکار | محموله کلیدی | الگوی ارتباطی |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| EVT-066 | **WorkflowStarted** | v1.0 | مدیریت جریان کار | عامل‌ها، مشاهده‌پذیری | شروع یک نمونه جدید از گردش‌کار | یک نمونه جدید از گردش‌کار شروع شد. | `workflow_instance_id`, `workflow_definition_id`, `version`, `triggered_by`, `tenant_id` | غیرهمزمان |
| EVT-067 | **WorkflowStepCompleted** | v1.0 | مدیریت جریان کار | مشاهده‌پذیری | تکمیل موفق یک گام از گردش‌کار | یک گام از گردش‌کار با موفقیت تکمیل شد. | `workflow_instance_id`, `step_id`, `step_type`, `result_summary`, `duration_ms` | غیرهمزمان |
| EVT-068 | **WorkflowStepFailed** | v1.0 | مدیریت جریان کار | مشاهده‌پذیری، حافظه و تجربه | بروز خطا در یک گام از گردش‌کار | یک گام از گردش‌کار با خطا مواجه شد. | `workflow_instance_id`, `step_id`, `error_code`, `error_message`, `retry_count` | غیرهمزمان |
| EVT-069 | **WorkflowCompleted** | v1.0 | مدیریت جریان کار | حافظه و تجربه، مشاهده‌پذیری | تکمیل موفق گردش‌کار | گردش‌کار با موفقیت به پایان رسید. | `workflow_instance_id`, `final_result`, `total_duration_ms`, `step_count` | غیرهمزمان |
| EVT-070 | **WorkflowFailed** | v1.0 | مدیریت جریان کار | حافظه و تجربه، مشاهده‌پذیری، عملیات | بروز خطای غیرقابل بازیابی در گردش‌کار | گردش‌کار با خطا مواجه شد. | `workflow_instance_id`, `error_code`, `error_message`, `failed_step_id` | غیرهمزمان |
| EVT-071 | **HumanTaskCreated** | v1.0 | مدیریت جریان کار | مشاهده‌پذیری | ایجاد یک Task انسانی جدید در گردش‌کار | یک Task انسانی جدید در گردش‌کار ایجاد شد. | `workflow_instance_id`, `task_id`, `assignee`, `task_type`, `timeout` | غیرهمزمان |
| EVT-072 | **HumanTaskCompleted** | v1.0 | مدیریت جریان کار | مشاهده‌پذیری | تکمیل موفق یک Task انسانی | یک Task انسانی با موفقیت تکمیل شد. | `workflow_instance_id`, `task_id`, `decision`, `completed_by` | غیرهمزمان |
| EVT-073 | **HumanTaskTimeout** | v1.0 | مدیریت جریان کار | مشاهده‌پذیری، عملیات | انقضای زمان Task انسانی | یک Task انسانی به Timeout رسید. | `workflow_instance_id`, `task_id`, `timeout_seconds` | غیرهمزمان |
| EVT-041 | **AgentTaskRequested** | v1.0 | مدیریت جریان کار | عامل‌ها و هماهنگی | یک گام Workflow از نوع Agent Task آماده اجراست | مدیریت جریان کار درخواست اجرای یک زیروظیفه را به‌صورت غیرهمزمان به Agent Orchestration Engine واگذار کرد. | `workflow_instance_id`, `step_id`, `task_definition`, `context_reference`, `correlation_id`, `ack_timeout_seconds` | غیرهمزمان (با Acknowledgment) |
| EVT-042 | **AgentTaskAcknowledged** | v1.0 | عامل‌ها و هماهنگی | مدیریت جریان کار | دریافت و تأیید رویداد `AgentTaskRequested` توسط Agent Orchestration Engine | Agent Orchestration Engine تأیید دریافت Task را به Workflow Engine ارسال کرد. | `workflow_instance_id`, `step_id`, `correlation_id`, `ack_timestamp` | غیرهمزمان |
| EVT-043 | **AgentTaskTimeout** | v1.0 | عامل‌ها و هماهنگی | مدیریت جریان کار، مشاهده‌پذیری | عدم دریافت تأیید یا نتیجه Task در مدت زمان مشخص | Task واگذارشده به Agent به Timeout رسید و Workflow باید تصمیم‌گیری کند. | `workflow_instance_id`, `step_id`, `correlation_id`, `timeout_seconds` | غیرهمزمان |
| EVT-025 | **CompensationFailed** | v1.0 | مدیریت جریان کار | مشاهده‌پذیری، حاکمیت و انطباق، عملیات | شکست در عملیات جبرانی یک گام گردش‌کار | عملیات جبرانی با وجود Retry موفق نشد و گردش‌کار نیاز به مداخله دستی دارد. | `workflow_instance_id`, `step_id`, `tool_id`, `error_message`, `retry_count`, `dlq_reference` | غیرهمزمان |

> **ارجاع:** رویدادهای کامل در سند `Domain Events Catalog` ثبت شده‌اند.

---

## ۸. وابستگی‌های دامنه

| دامنه تأمین‌کننده | نوع وابستگی | شدت | شرح |
| :--- | :--- | :--- | :--- |
| **عامل‌ها** | سرویس — زمان اجرا | قوی | واگذاری گام‌های Agent به عامل‌های هوشمند (با مکانیزم Acknowledgment) |
| **ابزارها** | سرویس — زمان اجرا | قوی | اجرای گام‌های عملیاتی از طریق ابزارها و عملیات جبرانی |
| **حافظه و تجربه** | سرویس — زمان اجرا | متوسط | ذخیره تاریخچه و نتایج گردش‌کار برای یادگیری |
| **هویت و دسترسی** | امنیتی — زمان اجرا | قوی | کنترل دسترسی به گردش‌کارها و تخصیص Taskهای انسانی |
| **حاکمیت و انطباق** | سرویس — زمان اجرا | قوی | اعمال Policyهای محدودیت و زمان‌بندی |
| **مشاهده‌پذیری** | رویدادی — زمان اجرا | ضعیف | ارسال Metrics و لاگ‌های گردش‌کار |
| **زیرساخت و عملیات** | سرویس — زمان اجرا | متوسط | استفاده از پایگاه داده و منابع پردازشی برای State Manager |

> **ارجاع:** وابستگی‌های کامل در سند `Domain Dependency Map` ثبت شده‌اند.

---

## ۹. ارتباط با سایر اسناد (References)

| نام سند | نوع ارتباط |
| :--- | :--- |
| `Naming_And_Terminology_Glossary` | مرجع واژه‌نامه و استاندارد نام‌گذاری |
| `Domain Landscape` | مرجع دامنه‌های اصلی و جایگاه این دامنه |
| `Context_Map` | مرجع الگوهای تعامل بین دامنه‌ها (به‌ویژه تعامل با عامل‌ها) |
| `Domain_Dependency_Map` | مرجع وابستگی‌های این دامنه به سایر دامنه‌ها |
| `Business_Rule_Catalog` | مرجع قوانین کسب‌وکار مرتبط |
| `Domain_Events_Catalog` | مرجع رویدادهای دامنه |
| `Architecture_Overview_Enterprise_AI_Platform` | مرجع Componentها و لایه‌های معماری |
| `Component_Design_Action_Tool_Engine` | مرجع طراحی Action & Tool Engine که Workflow Engine در آن قرار دارد |
| `Agent_Domain_Architecture` | مرجع معماری دامنه عامل‌ها که با این دامنه تعامل دارد |
| `Tool_Domain_Architecture` | مرجع معماری دامنه ابزارها که گام‌های عملیاتی و عملیات جبرانی را اجرا می‌کند |
| `Memory_Experience_Domain_Architecture` | مرجع دامنه حافظه و تجربه که نتایج گردش‌کار را ذخیره می‌کند |
| `Security_and_Privacy_Architecture` | مرجع کنترل‌های امنیتی و دسترسی |
| `Monitoring_and_Observability_Overview` | مرجع پایش و مشاهده‌پذیری |
| `Non_Functional_Requirements_And_SLA` | مرجع سیاست یکپارچه Retry و Timeout که در بخش ۴.۶ به آن ارجاع داده شده است |
| `Transaction_And_Compensation_Strategy` | مرجع الگوی Orchestrated Saga و مدیریت جبران که در بخش ۴.۶ به آن ارجاع داده شده است |
| `Event_&_Webhook_Domain_Architecture` | مرجع مکانیزم Acknowledgment برای رویدادها که در تعامل با عامل‌ها استفاده می‌شود |

---

## ۱۰. نتیجه‌گیری

دامنه **مدیریت جریان کار** به‌عنوان **هماهنگ‌کننده فرآیندهای سازمانی**، نقش حیاتی در خودکارسازی، مدیریت و پایش فرآیندهای چندمرحله‌ای کسب‌وکار ایفا می‌کند. معماری تعریف‌شده در این سند، چارچوبی استاندارد، انعطاف‌پذیر، مقیاس‌پذیر و قابل اعتماد برای تعریف، اجرا، پایش و مدیریت گردش‌کارهای پیچیده سازمانی فراهم می‌آورد.

با اضافه شدن مکانیزم‌های کلیدی مانند Snapshotting برای بازیابی وضعیت، یکپارچگی با سیاست Retry و Timeout سراسری، الگوی Orchestrated Saga با پشتیبانی از Dead Letter Queue برای جبران ناموفق، و مکانیزم Acknowledgment برای تعامل با عامل‌ها، قابلیت اطمینان و تاب‌آوری این دامنه به‌طور قابل‌توجهی افزایش یافته است.

رعایت اصول طراحی (Flexibility، Traceability، Fault Tolerance، State Management، Human‑in‑the‑Loop) و پیاده‌سازی مؤلفه‌های کلیدی (Workflow Definition، Workflow Engine، State Manager، Task Management، Human‑in‑the‑Loop، Error Handling) تضمین می‌کند که پلتفرم بتواند فرآیندهای سازمانی را به‌صورت خودکار، شفاف، قابل اعتماد و با قابلیت ممیزی کامل مدیریت کند و در عین حال با عامل‌ها و ابزارها به‌طور یکپارچه هماهنگ شود.

**مسئولیت به‌روزرسانی:** تیم معماری به همراه تیم‌های توسعه، محصول و عملیات مسئول بازبینی دوره‌ای این سند، به‌روزرسانی آن بر اساس تغییرات نیازمندی‌های گردش‌کار، الگوهای جدید فرآیندی و بازخورد تیم‌های پیاده‌سازی هستند.

**ثبات سند:** این سند باید با تغییرات در معماری کلان، نیازمندی‌های فرآیندهای کسب‌وکار و الزامات یکپارچه‌سازی هم‌گام باشد. هر تغییر در گردش‌کارها یا سیاست‌های اجرایی باید در این سند منعکس شود تا سایر اسناد معماری و طراحی نیز به‌روزرسانی شوند.

</div>